---
phase: 03-qr-pwa
plan: "04"
type: execute
wave: 2
depends_on:
  - "03-02"
files_modified:
  - src/routes/_protected/connections.tsx
autonomous: true
requirements:
  - CONN-02
  - CONN-03

must_haves:
  truths:
    - "ユーザーは /connections にアクセスしてコネクション一覧を閲覧できる"
    - "コネクション一覧はカード形式で表示され、アバター・表示名・つながった日時が表示される"
    - "イベントコンテキストがある場合はイベント名・会場名・日付もカードに表示される"
    - "カードをクリックすると相手の公開プロフィール（/u/$urlId）に遷移する"
    - "コネクションが0件の場合は空状態メッセージが表示される"
  artifacts:
    - path: "src/routes/_protected/connections.tsx"
      provides: "/connections ページ（コネクション一覧）"
      contains: "createFileRoute('/_protected/connections')"
  key_links:
    - from: "src/routes/_protected/connections.tsx"
      to: "src/server/functions/connection.ts getMyConnections"
      via: "Route loader"
      pattern: "getMyConnections"
    - from: "ConnectionCard"
      to: "/u/$urlId route"
      via: "Link to={`/u/${conn.toUrlId}`}"
      pattern: "Link.*toUrlId"
---

<objective>
/connections ページを作成し、自分がつながった相手の一覧をカード形式で表示する。CONTEXT.mdのロック済み決定に従い、/me には表示せず専用ページに独立する。

Purpose: コネクション一覧の閲覧（CONN-02, CONN-03）
Output: /connections ルート（コネクションカード一覧・空状態・イベントコンテキスト表示）
</objective>

<execution_context>
@C:/Users/pitan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pitan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server/functions/connection.ts
@src/routes/_protected/me.tsx
@.planning/phases/03-qr-pwa/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: /connections ページ実装</name>
  <files>src/routes/_protected/connections.tsx</files>
  <action>
src/routes/_protected/connections.tsx を新規作成する。_protected レイアウト配下のため認証は自動保証される。

以下の実装を行う:

```typescript
import { createFileRoute, Link } from '@tanstack/react-router'
import { getMyConnections } from '../../server/functions/connection'
import { InitialsAvatar } from '../../components/InitialsAvatar'

export const Route = createFileRoute('/_protected/connections')({
  loader: () => getMyConnections(),
  component: ConnectionsPage,
})

function ConnectionsPage() {
  const connections = Route.useLoaderData()

  return (
    <div className="min-h-screen flex flex-col">
      {/* ヘッダー */}
      <div className="flex items-center justify-between p-4 border-b">
        <h1 className="text-lg font-bold">つながり</h1>
        <Link to="/me" className="text-sm text-gray-500 underline">戻る</Link>
      </div>

      {/* コネクション一覧 */}
      <div className="flex-1 p-4">
        {connections.length === 0 ? (
          <EmptyState />
        ) : (
          <div className="flex flex-col gap-3">
            {connections.map((conn) => (
              <ConnectionCard key={conn.connectionId} conn={conn} />
            ))}
          </div>
        )}
      </div>
    </div>
  )
}

type Connection = Awaited<ReturnType<typeof getMyConnections>>[number]

function ConnectionCard({ conn }: { conn: Connection }) {
  const connectedDate = new Date(conn.connectedAt).toLocaleDateString('ja-JP', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  })

  return (
    <Link
      to="/u/$urlId"
      params={{ urlId: conn.toUrlId }}
      className="flex items-start gap-3 p-4 bg-white rounded-xl border hover:bg-gray-50 transition-colors"
    >
      {/* アバター */}
      <div className="flex-shrink-0">
        {conn.toAvatarUrl ? (
          <img src={conn.toAvatarUrl} alt="" className="w-12 h-12 rounded-full object-cover" />
        ) : (
          <InitialsAvatar name={conn.toDisplayName} size={48} />
        )}
      </div>

      {/* 情報 */}
      <div className="flex-1 min-w-0">
        <p className="font-medium text-gray-900 truncate">{conn.toDisplayName}</p>
        <p className="text-xs text-gray-400 mt-0.5">{connectedDate} につながった</p>

        {/* イベントコンテキスト（チェックイン中につながった場合のみ表示） */}
        {conn.eventName && (
          <div className="mt-1.5 px-2 py-1 bg-pink-50 rounded text-xs text-pink-700">
            <span className="font-medium">{conn.eventName}</span>
            {conn.venueName && <span className="text-pink-500"> @ {conn.venueName}</span>}
            {conn.eventDate && (
              <span className="text-pink-400">
                {' '}({new Date(conn.eventDate).toLocaleDateString('ja-JP', { month: 'short', day: 'numeric' })})
              </span>
            )}
          </div>
        )}
      </div>

      {/* 矢印 */}
      <div className="flex-shrink-0 text-gray-300 self-center">›</div>
    </Link>
  )
}

function EmptyState() {
  return (
    <div className="flex flex-col items-center justify-center py-20 gap-4">
      <div className="text-4xl">🤝</div>
      <p className="text-sm text-gray-500 text-center">
        まだつながりがありません
      </p>
      <p className="text-xs text-gray-400 text-center">
        相手のQRコードを読み取り、プロフィールページで「つながる」を押してみましょう
      </p>
    </div>
  )
}
```

注意:
- `connections` の `connectedAt` は `Date | string` 型になる可能性があるため、`new Date(conn.connectedAt)` で安全にDateオブジェクト化する
- コネクションのソート順は getMyConnections が `desc(connections.connectedAt)` で返すため、UIでの再ソートは不要
- /me への「戻る」リンクを追加（ナビゲーション）
- CONTEXT.md locked: コネクション一覧は /connections のみ（/me には表示しない）
  </action>
  <verify>
    <automated>cd C:/Users/pitan/dev/nafuda && pnpm tsc --noEmit 2>&1 | grep -i "connections" | head -20 || echo "TYPE_CHECK_CLEAN"</automated>
    <manual>grep "createFileRoute\|ConnectionCard\|getMyConnections" src/routes/_protected/connections.tsx を確認</manual>
  </verify>
  <done>src/routes/_protected/connections.tsx が作成され、/connections ルートが存在する。コネクション一覧が connectedAt 降順で表示される。イベントコンテキストがある場合はピンク色バッジで表示される。空状態メッセージが存在する。TypeScriptエラーなし。</done>
</task>

</tasks>

<verification>
```bash
cd C:/Users/pitan/dev/nafuda
# TypeScript型チェック
pnpm tsc --noEmit

# ファイル存在確認
ls src/routes/_protected/connections.tsx

# 必要な要素が含まれているか確認
grep -n "getMyConnections\|ConnectionCard\|EmptyState\|/u/\$urlId" src/routes/_protected/connections.tsx
```
</verification>

<success_criteria>
- src/routes/_protected/connections.tsx が存在し、/_protected/connections ルートとして機能する
- コネクション一覧がカード形式で表示される（アバター・表示名・日時）
- イベントコンテキスト（eventName, venueName, eventDate）がある場合はカードに表示される
- カードクリックで /u/$urlId に遷移する
- 0件の場合は空状態メッセージが表示される
- pnpm tsc --noEmit がエラーなし
</success_criteria>

<output>
After completion, create `.planning/phases/03-qr-pwa/03-04-SUMMARY.md`
</output>
