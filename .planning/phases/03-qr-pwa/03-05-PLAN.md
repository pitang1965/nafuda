---
phase: 03-qr-pwa
plan: "05"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/sw.ts
  - vite.config.ts
  - src/hooks/usePwaInstall.ts
  - src/components/PwaInstallBanner.tsx
  - src/routes/__root.tsx
  - src/routes/_protected/me.tsx
autonomous: true
requirements:
  - PWA-01
  - PWA-02

must_haves:
  truths:
    - "pnpm build で dist/sw.js が生成される（Workbox カスタムプラグイン経由）"
    - "ブラウザが /me ページを一度開けば Service Worker がキャッシュし、機内モードでも /me が表示される"
    - "Chrome/Android では PWA インストールバナーが表示される（beforeinstallprompt）"
    - "iOS Safari では「Safariのシェアボタン → ホーム画面に追加」の手動案内バナーが表示される"
    - "/me ページの QRボトムシート付近に PwaInstallBanner が表示される"
  artifacts:
    - path: "src/sw.ts"
      provides: "Service Worker ソース（Workbox injectManifest 用）"
      contains: "precacheAndRoute"
    - path: "vite.config.ts"
      provides: "カスタム PWA Vite プラグイン（workbox-build injectManifest）"
      contains: "nafuda-pwa"
    - path: "src/hooks/usePwaInstall.ts"
      provides: "PWA インストールプロンプトフック"
      exports: ["usePwaInstall"]
    - path: "src/components/PwaInstallBanner.tsx"
      provides: "インストール誘導バナー（Chrome/iOS 両対応）"
      exports: ["PwaInstallBanner"]
    - path: "src/routes/_protected/me.tsx"
      provides: "/me ページ（03-01 で作成済み）に PwaInstallBanner を追加"
      contains: "PwaInstallBanner"
  key_links:
    - from: "vite.config.ts nafuda-pwa plugin"
      to: "workbox-build injectManifest"
      via: "writeBundle hook"
      pattern: "injectManifest.*swDest"
    - from: "src/routes/__root.tsx"
      to: "/sw.js"
      via: "navigator.serviceWorker.register in useEffect"
      pattern: "serviceWorker.*register.*sw.js"
    - from: "src/routes/_protected/me.tsx"
      to: "src/components/PwaInstallBanner.tsx"
      via: "import + render near QRBottomSheet"
      pattern: "PwaInstallBanner"
---

<objective>
カスタム Workbox Vite プラグインで Service Worker を生成し、/me ページのオフライン表示を実現する。PWA インストールプロンプトフックを作成し、Chrome/Android と iOS Safari の両方に対応したインストール誘導バナーを実装する。完成した PwaInstallBanner を /me ページの QRボトムシート付近に組み込む。

Purpose: QRコードオフライン表示（PWA-02）とホーム画面インストール（PWA-01）を実現する
Output: dist/sw.js（ビルド時生成）、usePwaInstall フック、PwaInstallBanner コンポーネント、me.tsx への組み込み
</objective>

<execution_context>
@C:/Users/pitan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pitan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@vite.config.ts
@src/routes/__root.tsx
@src/routes/_protected/me.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service Worker ソース + カスタム Vite プラグイン</name>
  <files>src/sw.ts, vite.config.ts</files>
  <action>
**vite-plugin-pwa は使用禁止** (TanStack Start + Vite 6 と非互換 — Phase 3 RESEARCH.md Pitfall 1 参照)。
代わりに `workbox-build` の `injectManifest` API を使うカスタム Vite プラグインを実装する。

**Step 1: src/sw.ts を作成する**

```typescript
// src/sw.ts — Service Worker ソース
// workbox-build injectManifest がこのファイルにプレキャッシュマニフェストを注入する
import { precacheAndRoute, cleanupOutdatedCaches } from 'workbox-precaching'
import { registerRoute, NavigationRoute } from 'workbox-routing'
import { NetworkFirst, CacheFirst } from 'workbox-strategies'

declare let self: ServiceWorkerGlobalScope & { __WB_MANIFEST: Array<{ url: string; revision: string | null }> }

// プレキャッシュ: ビルド時にworkbox-buildが注入するマニフェスト
precacheAndRoute(self.__WB_MANIFEST)
cleanupOutdatedCaches()

// skipWaiting + clientsClaim: 新バージョンのSWをすぐに有効化
self.addEventListener('install', () => self.skipWaiting())
self.addEventListener('activate', (event) => {
  event.waitUntil(self.clients.claim())
})

// /me ページ: NetworkFirst（オフライン時はキャッシュから応答）
registerRoute(
  ({ url }) => url.pathname === '/me' || url.pathname.startsWith('/me'),
  new NetworkFirst({
    cacheName: 'nafuda-pages',
    networkTimeoutSeconds: 3,
    plugins: [],
  })
)

// 静的アセット: CacheFirst（JS/CSS/画像）
registerRoute(
  ({ request }) => request.destination === 'script' || request.destination === 'style' || request.destination === 'image',
  new CacheFirst({
    cacheName: 'nafuda-assets',
  })
)
```

**Step 2: vite.config.ts にカスタムプラグインを追加する**

既存の vite.config.ts を読み込んでから修正する。以下のプラグインを `plugins` 配列に追加する（`tanstackStart()` の後に配置）:

```typescript
import type { Plugin } from 'vite'
import { injectManifest } from 'workbox-build'
import path from 'path'

function nafudaPwaPlugin(): Plugin {
  return {
    name: 'nafuda-pwa',
    apply: 'build',   // build 時のみ実行（dev では不要）
    async writeBundle() {
      const swSrc = path.resolve('src/sw.ts')
      const swDest = path.resolve('dist/sw.js')
      const globDirectory = path.resolve('dist')

      try {
        const { count, size, warnings } = await injectManifest({
          swSrc,
          swDest,
          globDirectory,
          globPatterns: ['**/*.{js,css,html,png,svg,webmanifest,ico}'],
          // TanStack Start SSRでは navigateFallback は不要（サーバーがHTMLを返す）
          // フロントエンドアセットのみプレキャッシュ
        })
        console.log(`[nafuda-pwa] SW generated: ${count} files (${Math.round(size / 1024)}KB)`)
        if (warnings.length) console.warn('[nafuda-pwa] Warnings:', warnings)
      } catch (err) {
        console.error('[nafuda-pwa] SW generation failed:', err)
        throw err
      }
    },
  }
}
```

既存の vite.config.ts に追加後のプラグイン配列:
```typescript
plugins: [
  cloudflare({ viteEnvironment: { name: 'ssr' } }),
  tailwindcss(),
  tanstackStart(),
  viteReact(),
  nafudaPwaPlugin(),  // ← 追加
],
```

また、vite.config.ts の先頭に以下のインポートを追加する:
```typescript
import type { Plugin } from 'vite'
import { injectManifest } from 'workbox-build'
import path from 'node:path'
```

注意: `workbox-build` は既に node_modules にインストール済み（RESEARCH.md 確認済み）。追加インストール不要。
  </action>
  <verify>
    <automated>cd C:/Users/pitan/dev/nafuda && pnpm build 2>&1 | grep -E "\[nafuda-pwa\]|SW generated|error" | head -20</automated>
    <manual>pnpm build 後に dist/sw.js が存在することを確認: ls -la dist/sw.js</manual>
  </verify>
  <done>src/sw.ts が作成され、vite.config.ts にカスタムプラグインが追加されている。pnpm build 後に dist/sw.js が生成される。ビルドログに "[nafuda-pwa] SW generated" が表示される。</done>
</task>

<task type="auto">
  <name>Task 2: PWA インストールフック + バナーコンポーネント + SW 登録 + me.tsx への組み込み</name>
  <files>src/hooks/usePwaInstall.ts, src/components/PwaInstallBanner.tsx, src/routes/__root.tsx, src/routes/_protected/me.tsx</files>
  <action>
**Step 1: src/hooks/usePwaInstall.ts を作成**

```typescript
import { useState, useEffect } from 'react'

// BeforeInstallPromptEvent は標準ライブラリに含まれないため手動定義
interface BeforeInstallPromptEvent extends Event {
  readonly platforms: string[]
  readonly userChoice: Promise<{ outcome: 'accepted' | 'dismissed'; platform: string }>
  prompt(): Promise<void>
}

export function usePwaInstall() {
  const [installPrompt, setInstallPrompt] = useState<BeforeInstallPromptEvent | null>(null)
  const [isInstalled, setIsInstalled] = useState(false)

  useEffect(() => {
    // iOS Safari のチェック（beforeinstallprompt が発火しない）
    const isIos = /iphone|ipad|ipod/i.test(navigator.userAgent)
    const isInStandaloneMode = ('standalone' in navigator) && (navigator as { standalone?: boolean }).standalone === true
    if (isIos && isInStandaloneMode) {
      setIsInstalled(true)
      return
    }

    const handler = (e: Event) => {
      e.preventDefault()
      setInstallPrompt(e as BeforeInstallPromptEvent)
    }

    window.addEventListener('beforeinstallprompt', handler)

    // インストール後にフラグを更新
    window.addEventListener('appinstalled', () => {
      setInstallPrompt(null)
      setIsInstalled(true)
    })

    return () => {
      window.removeEventListener('beforeinstallprompt', handler)
    }
  }, [])

  const promptInstall = async () => {
    if (!installPrompt) return
    await installPrompt.prompt()
    const choice = await installPrompt.userChoice
    if (choice.outcome === 'accepted') {
      setInstallPrompt(null)
    }
  }

  // iOS Safari 判定（コンポーネントで異なるUIを表示するため）
  const isIos = typeof navigator !== 'undefined' && /iphone|ipad|ipod/i.test(navigator.userAgent)
  const isInStandaloneMode = typeof navigator !== 'undefined' && ('standalone' in navigator) && (navigator as { standalone?: boolean }).standalone === true

  return {
    canInstall: !!installPrompt,      // Chrome/Android: beforeinstallprompt あり
    isIos: isIos && !isInStandaloneMode,  // iOS Safari でスタンドアロンでない
    isInstalled,
    promptInstall,
  }
}
```

**Step 2: src/components/PwaInstallBanner.tsx を作成**

QRコード表示時に表示する軽量バナー（CONTEXT.md: Claude の裁量でデザイン決定）:

```tsx
import { usePwaInstall } from '../hooks/usePwaInstall'

export function PwaInstallBanner() {
  const { canInstall, isIos, isInstalled, promptInstall } = usePwaInstall()

  // インストール済み or 何も表示するものがない場合は非表示
  if (isInstalled) return null

  // iOS Safari: 手動インストール案内
  if (isIos) {
    return (
      <div className="mx-4 mb-4 p-3 bg-blue-50 rounded-xl text-xs text-blue-700 text-center">
        Safari のシェアボタン（↑）→「ホーム画面に追加」でオフライン表示できます
      </div>
    )
  }

  // Chrome/Android: beforeinstallprompt バナー
  if (canInstall) {
    return (
      <div className="mx-4 mb-4 flex items-center justify-between p-3 bg-gray-50 rounded-xl">
        <p className="text-xs text-gray-600">ホーム画面に追加するとオフラインでも QR を表示できます</p>
        <button
          onClick={promptInstall}
          className="ml-3 flex-shrink-0 px-3 py-1.5 bg-black text-white rounded-lg text-xs font-medium"
        >
          追加する
        </button>
      </div>
    )
  }

  return null
}
```

**Step 3: src/routes/__root.tsx に SW 登録を追加**

まず __root.tsx の現在の内容を読み込む。`useEffect` で Service Worker を登録するクライアント専用コードを追加する。既存の `__root.tsx` の ClientOnly 部分（または useEffect 内）に追加:

```typescript
// __root.tsx の適切な場所に追加
import { useEffect } from 'react'

// 既存のRootComponent または RootDocument コンポーネント内の useEffect に追加:
useEffect(() => {
  if ('serviceWorker' in navigator && typeof window !== 'undefined') {
    // load イベント後に登録してパフォーマンスへの影響を最小化
    window.addEventListener('load', () => {
      navigator.serviceWorker.register('/sw.js', { scope: '/' })
        .then((reg) => console.log('[SW] Registered:', reg.scope))
        .catch((err) => console.warn('[SW] Registration failed:', err))
    })
  }
}, [])
```

注意: TanStack Start SSR 環境では `typeof window !== 'undefined'` チェックが必須。useEffect 内のため実際には常にブラウザ環境だが、型安全のため記述する。

**Step 4: src/routes/_protected/me.tsx に PwaInstallBanner を組み込む**

03-01 で作成済みの me.tsx を読み込み、以下の変更を加える:

1. ファイル先頭に import を追加する:
   ```tsx
   import { PwaInstallBanner } from '../../components/PwaInstallBanner'
   ```

2. JSX 内の QRボトムシート付近（`<QRBottomSheet ... />` の直前または直後）に `<PwaInstallBanner />` を配置する。
   推奨位置: `<QRBottomSheet />` の直前（ボタンより下、ボトムシートより上）:
   ```tsx
   {/* PWA インストール誘導バナー（QRコードボタンの下に配置） */}
   <PwaInstallBanner />

   {currentPersona && urlId && (
     <QRBottomSheet
       isOpen={qrOpen}
       onClose={() => setQrOpen(false)}
       urlId={urlId}
       shareToken={currentPersona.shareToken}
       displayName={currentPersona.displayName}
     />
   )}
   ```

   me.tsx の既存コードを壊さないよう、必ず現在のファイル内容を確認してから編集すること。
   PwaInstallBanner は Chrome/Android では `beforeinstallprompt` が発火した時のみ、iOS Safari では Safari ブラウザ判定時のみ表示される（それ以外は null を返す）ため、常時レンダリングして問題ない。
  </action>
  <verify>
    <automated>cd C:/Users/pitan/dev/nafuda && pnpm tsc --noEmit 2>&1 | grep -E "usePwaInstall|PwaInstallBanner|sw.ts" | head -20 || echo "TYPE_CHECK_CLEAN"</automated>
    <manual>grep -n "PwaInstallBanner" src/routes/_protected/me.tsx を実行し、import と render の両方が存在することを確認</manual>
  </verify>
  <done>src/hooks/usePwaInstall.ts と src/components/PwaInstallBanner.tsx が作成されている。src/routes/__root.tsx に SW 登録 useEffect が追加されている。src/routes/_protected/me.tsx に PwaInstallBanner の import と render が追加されている。TypeScriptエラーなし。</done>
</task>

</tasks>

<verification>
```bash
cd C:/Users/pitan/dev/nafuda

# TypeScript 型チェック
pnpm tsc --noEmit

# ファイル存在確認
ls src/sw.ts src/hooks/usePwaInstall.ts src/components/PwaInstallBanner.tsx

# me.tsx に PwaInstallBanner が組み込まれていることを確認
grep -n "PwaInstallBanner" src/routes/_protected/me.tsx

# ビルド + SW 生成確認
pnpm build 2>&1 | grep -E "\[nafuda-pwa\]|Generated|error" | head -20

# dist/sw.js の存在確認（ビルド後）
ls -la dist/sw.js 2>/dev/null && echo "SW_EXISTS" || echo "SW_MISSING"

# SW ファイルサイズが妥当か確認（0バイトでないことを確認）
wc -l dist/sw.js 2>/dev/null || echo "SW_NOT_BUILT_YET"
```
</verification>

<success_criteria>
- src/sw.ts が存在する（precacheAndRoute, NetworkFirst for /me, CacheFirst for assets）
- vite.config.ts に nafudaPwaPlugin が追加されている（vite-plugin-pwa は使用しない）
- pnpm build で dist/sw.js が生成される
- src/hooks/usePwaInstall.ts が usePwaInstall フックをエクスポートする
- src/components/PwaInstallBanner.tsx が Chrome/Android（beforeinstallprompt）と iOS Safari（手動案内）の両方に対応している
- src/routes/__root.tsx に SW 登録 useEffect が追加されている
- src/routes/_protected/me.tsx に PwaInstallBanner の import と render が追加されている（QRボトムシート付近）
- pnpm tsc --noEmit がエラーなし
</success_criteria>

<output>
After completion, create `.planning/phases/03-qr-pwa/03-05-SUMMARY.md`
</output>
