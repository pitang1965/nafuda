---
phase: 03-qr-pwa
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/db/schema.ts
  - src/server/functions/connection.ts
autonomous: true
requirements:
  - CONN-02
  - CONN-03

must_haves:
  truths:
    - "connections テーブルが DB スキーマに定義されている"
    - "createConnection サーバー関数が存在し、未ログインユーザーは 401 エラーになる"
    - "自分自身への接続（fromPersonaId === toPersonaId）はサーバー側で拒否される"
    - "チェックイン中の場合はイベントコンテキスト（eventName・venueName・eventDate）がコネクションに付与される"
    - "同一ペルソナペアの重複コネクションは DB UNIQUE 制約で防止される"
    - "getMyConnections でコネクション一覧（相手プロフィール情報含む）を取得できる"
  artifacts:
    - path: "src/server/db/schema.ts"
      provides: "connections テーブル定義"
      contains: "export const connections = pgTable('connections'"
    - path: "src/server/functions/connection.ts"
      provides: "createConnection / getMyConnections サーバー関数"
      exports: ["createConnection", "getMyConnections"]
  key_links:
    - from: "src/server/functions/connection.ts"
      to: "src/server/db/schema.ts"
      via: "connections table import"
      pattern: "import.*connections.*schema"
    - from: "createConnection handler"
      to: "getActiveCheckin pattern (event.ts)"
      via: "eventCheckins JOIN events query for active checkin context"
      pattern: "isNull.*checkedOutAt"
---

<objective>
connections テーブルを Drizzle スキーマに追加し、createConnection / getMyConnections サーバー関数を実装する。コネクション記録はチェックイン中の場合にイベントコンテキストを自動付与する。

Purpose: 「つながる」ボタンからのコネクション記録（CONN-02）とイベントコンテキスト付与（CONN-03）の基盤を提供する
Output: schema.ts 更新（connections テーブル）、connection.ts（サーバー関数2件）
</objective>

<execution_context>
@C:/Users/pitan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pitan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server/db/schema.ts
@src/server/functions/event.ts
@src/server/functions/profile.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: connections テーブルを schema.ts に追加</name>
  <files>src/server/db/schema.ts</files>
  <action>
src/server/db/schema.ts の末尾に connections テーブルを追加する。既存インポート行に `unique` を追加する（既存のインポートに含まれていない場合）。

追加するテーブル定義:
```typescript
import { pgTable, text, boolean, timestamp, uuid, jsonb, smallint, point, unique } from 'drizzle-orm/pg-core'

// 既存テーブルはそのまま維持する

// Connections table: one-way "connect" action from viewer to profile owner
export const connections = pgTable('connections', {
  id: uuid('id').primaryKey().defaultRandom(),
  // fromPersonaId: コネクションを記録した側（「つながる」を押した人のペルソナ）
  fromPersonaId: uuid('from_persona_id').notNull().references(() => personas.id),
  // toPersonaId: QRを見せた側（つながられた人のペルソナ）
  toPersonaId: uuid('to_persona_id').notNull().references(() => personas.id),
  fromUserId: text('from_user_id').notNull(),  // Better Auth user.id（認証チェック用）
  // イベントコンテキスト（チェックイン中でない場合は null）
  eventId: uuid('event_id').references(() => events.id),
  eventName: text('event_name'),    // 非正規化: JOIN不要で表示できるよう保存
  venueName: text('venue_name'),    // 非正規化
  eventDate: timestamp('event_date', { withTimezone: true }),
  connectedAt: timestamp('connected_at', { withTimezone: true }).defaultNow().notNull(),
}, (table) => ({
  // 同一ペルソナペアのコネクション重複防止（一方通行なのでfrom/to両方でユニーク）
  uniqueConn: unique().on(table.fromPersonaId, table.toPersonaId),
}))
```

注意: pgTable の第3引数でユニーク制約を定義するパターンはexisting schema（eventCheckins等）と同じ。`unique` は `drizzle-orm/pg-core` からインポートする。
  </action>
  <verify>
    <automated>cd C:/Users/pitan/dev/nafuda && pnpm tsc --noEmit 2>&1 | grep -i "schema" | head -20 || echo "TYPE_CHECK_CLEAN"</automated>
    <manual>grep "connections" src/server/db/schema.ts を確認</manual>
  </verify>
  <done>src/server/db/schema.ts に connections テーブルが定義され、TypeScriptエラーがない。</done>
</task>

<task type="auto">
  <name>Task 2: createConnection / getMyConnections サーバー関数を実装</name>
  <files>src/server/functions/connection.ts</files>
  <action>
src/server/functions/connection.ts を新規作成する。既存の event.ts のパターン（createServerFn + getRequest + auth.api.getSession）に従う。

```typescript
import { createServerFn } from '@tanstack/react-start'
import { getRequest } from '@tanstack/react-start/server'
import { z } from 'zod'
import { eq, and, isNull, desc } from 'drizzle-orm'
import { db } from '../db/client'
import { connections, personas, urlIds, eventCheckins, events } from '../db/schema'
import { auth } from '../auth'

// 「つながる」ボタンから呼ばれる: targetUrlId のペルソナにコネクションを記録する
export const createConnection = createServerFn({ method: 'POST' })
  .inputValidator(z.object({
    targetUrlId: z.string().min(1),  // 相手の urlId（公開プロフィールURLに使われる値）
  }))
  .handler(async ({ data }) => {
    // 1. 認証チェック
    const request = getRequest()
    const session = await auth.api.getSession({ headers: request.headers })
    if (!session?.user) throw new Error('Unauthorized')

    // 2. 呼び出し側のデフォルトペルソナを取得
    const fromPersonaRows = await db.select({ id: personas.id })
      .from(personas)
      .where(and(eq(personas.userId, session.user.id), eq(personas.isDefault, true)))
      .limit(1)
    if (!fromPersonaRows[0]) throw new Error('自分のペルソナが見つかりません')
    const fromPersonaId = fromPersonaRows[0].id

    // 3. 対象 urlId からペルソナを解決
    const targetUrlRow = await db.select({ userId: urlIds.userId })
      .from(urlIds)
      .where(eq(urlIds.urlId, data.targetUrlId))
      .limit(1)
    if (!targetUrlRow[0]) throw new Error('対象ユーザーが見つかりません')

    const toPersonaRows = await db.select({ id: personas.id })
      .from(personas)
      .where(and(eq(personas.userId, targetUrlRow[0].userId), eq(personas.isDefault, true)))
      .limit(1)
    if (!toPersonaRows[0]) throw new Error('対象ペルソナが見つかりません')
    const toPersonaId = toPersonaRows[0].id

    // 4. 自己接続防止チェック
    if (fromPersonaId === toPersonaId) throw new Error('自分自身にはつながれません')

    // 5. アクティブチェックイン確認（イベントコンテキスト付与用）
    const activeCheckinRows = await db.select({
      eventId: eventCheckins.eventId,
      eventName: events.name,
      venueName: events.venueName,
      eventDate: events.eventDate,
    })
      .from(eventCheckins)
      .innerJoin(events, eq(eventCheckins.eventId, events.id))
      .where(and(
        eq(eventCheckins.personaId, fromPersonaId),
        isNull(eventCheckins.checkedOutAt)
      ))
      .limit(1)
    const activeCheckin = activeCheckinRows[0] ?? null

    // 6. connections に INSERT（UNIQUE 制約違反 = 重複 → 23505 エラーをキャッチして無視）
    try {
      const newConn = await db.insert(connections)
        .values({
          fromPersonaId,
          toPersonaId,
          fromUserId: session.user.id,
          eventId: activeCheckin?.eventId ?? null,
          eventName: activeCheckin?.eventName ?? null,
          venueName: activeCheckin?.venueName ?? null,
          eventDate: activeCheckin?.eventDate ?? null,
        })
        .returning()
      return { connection: newConn[0], alreadyConnected: false }
    } catch (err: unknown) {
      // PostgreSQL UNIQUE constraint violation
      if (err && typeof err === 'object' && 'code' in err && err.code === '23505') {
        return { connection: null, alreadyConnected: true }
      }
      throw err
    }
  })

// 自分がつながった相手の一覧を取得（connectedAt 降順）
export const getMyConnections = createServerFn({ method: 'GET' })
  .handler(async () => {
    const request = getRequest()
    const session = await auth.api.getSession({ headers: request.headers })
    if (!session?.user) throw new Error('Unauthorized')

    // 自分のデフォルトペルソナを取得
    const myPersonaRows = await db.select({ id: personas.id })
      .from(personas)
      .where(and(eq(personas.userId, session.user.id), eq(personas.isDefault, true)))
      .limit(1)
    if (!myPersonaRows[0]) return []
    const myPersonaId = myPersonaRows[0].id

    // connections + 相手ペルソナ情報 + urlId を JOIN して取得
    const result = await db.select({
      connectionId: connections.id,
      connectedAt: connections.connectedAt,
      eventName: connections.eventName,
      venueName: connections.venueName,
      eventDate: connections.eventDate,
      toPersonaId: connections.toPersonaId,
      toDisplayName: personas.displayName,
      toAvatarUrl: personas.avatarUrl,
      toUrlId: urlIds.urlId,
      toShareToken: personas.shareToken,
    })
      .from(connections)
      .innerJoin(personas, eq(connections.toPersonaId, personas.id))
      .innerJoin(urlIds, eq(personas.userId, urlIds.userId))
      .where(eq(connections.fromPersonaId, myPersonaId))
      .orderBy(desc(connections.connectedAt))

    return result
  })
```

注意:
- `getRequest()` は `@tanstack/react-start/server` からのみインポート（Phase 01-02 確立済みパターン）
- UNIQUE違反の `23505` catch パターンは Phase 02-01 で確立済み（checkinToEvent と同じ）
- イベントコンテキストは非正規化で保存（JOIN不要で表示できる）
  </action>
  <verify>
    <automated>cd C:/Users/pitan/dev/nafuda && pnpm tsc --noEmit 2>&1 | grep -i "connection" | head -20 || echo "TYPE_CHECK_CLEAN"</automated>
    <manual>grep "createConnection\|getMyConnections" src/server/functions/connection.ts を確認</manual>
  </verify>
  <done>src/server/functions/connection.ts が作成され、createConnection と getMyConnections がエクスポートされ、TypeScriptエラーがない。</done>
</task>

</tasks>

<verification>
```bash
cd C:/Users/pitan/dev/nafuda
# TypeScript型チェック（スキーマ + 関数）
pnpm tsc --noEmit

# connections テーブルがスキーマに存在することを確認
grep "connections" src/server/db/schema.ts

# サーバー関数が存在することを確認
ls src/server/functions/connection.ts

# Drizzle kit でスキーマ変更の差分確認（マイグレーションファイルは Plan 03 で実行）
pnpm db:generate --dry-run 2>&1 | head -20 || pnpm drizzle-kit generate --dry-run 2>&1 | head -20 || echo "DRY_RUN_NOT_SUPPORTED"
```
</verification>

<success_criteria>
- src/server/db/schema.ts に connections テーブルが定義されている（fromPersonaId, toPersonaId, fromUserId, eventId, eventName, venueName, eventDate, connectedAt, UNIQUE制約）
- src/server/functions/connection.ts に createConnection と getMyConnections がエクスポートされている
- createConnection: 未認証 → Unauthorized エラー、自己接続 → エラー、重複 → alreadyConnected: true、チェックイン中 → イベントコンテキスト付与
- pnpm tsc --noEmit がエラーなし
</success_criteria>

<output>
After completion, create `.planning/phases/03-qr-pwa/03-02-SUMMARY.md`
</output>
