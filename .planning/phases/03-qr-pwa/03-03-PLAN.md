---
phase: 03-qr-pwa
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-02"
files_modified:
  - src/routes/u/$urlId.tsx
autonomous: true
requirements:
  - CONN-02
  - CONN-03

must_haves:
  truths:
    - "pnpm db:generate でマイグレーションファイルが生成され、pnpm db:migrate でDBに適用される"
    - "公開プロフィールページ /u/$urlId に「つながる」ボタンが表示される（自分のプロフィール以外）"
    - "自分の /u/$urlId にアクセスした場合は「つながる」ボタンが表示されない"
    - "未ログインユーザーが「つながる」ボタンを押すとログイン画面に遷移する"
    - "ログインユーザーが「つながる」を押すとコネクションが記録され、成功フィードバック（トースト等）が表示される"
    - "すでにつながっている場合は「つながり済み」と表示される"
  artifacts:
    - path: "src/routes/u/$urlId.tsx"
      provides: "「つながる」ボタン付き公開プロフィールページ"
      contains: "createConnection"
  key_links:
    - from: "src/routes/u/$urlId.tsx"
      to: "src/server/functions/connection.ts createConnection"
      via: "button onClick → createConnection({ data: { targetUrlId } })"
      pattern: "createConnection.*targetUrlId"
    - from: "src/routes/u/$urlId.tsx"
      to: "/login route"
      via: "navigate({ to: '/login' }) when no session"
      pattern: "navigate.*login"
    - from: "src/routes/u/$urlId.tsx loader"
      to: "session.user.urlId"
      via: "loader で自分の urlId を取得し、params.urlId と比較して isOwnProfile を算出"
      pattern: "isOwnProfile"
---

<objective>
connections テーブルのDBマイグレーションを実行し、公開プロフィールページ（/u/$urlId）に「つながる」ボタンを追加する。自分のプロフィールでは「つながる」ボタンを非表示にする。

Purpose: ユーザーが相手のプロフィールを見た後に明示的に「つながる」を選択できる（CONN-02, CONN-03）
Output: Neon DBにconnectionsテーブル適用済み、/u/$urlId に「つながる」ボタン実装（自己プロフィールでは非表示）
</objective>

<execution_context>
@C:/Users/pitan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pitan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server/db/schema.ts
@src/server/functions/connection.ts
@src/routes/u/$urlId.tsx
@.planning/phases/03-qr-pwa/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DBマイグレーション実行</name>
  <files>drizzle/</files>
  <action>
以下の順番でDrizzleマイグレーションを実行する:

```bash
# 1. スキーマ変更からマイグレーションSQLファイルを生成
pnpm db:generate

# 2. Neon DBにマイグレーションを適用
pnpm db:migrate
```

実行後に確認すること:
- `drizzle/` ディレクトリに新しいSQLファイルが生成されている（connections テーブルのCREATE TABLE文が含まれる）
- `pnpm db:migrate` が `All migrations applied` またはエラーなしで完了する

もし DATABASE_URL が未設定でエラーになる場合:
- `.env.local` に `DATABASE_URL=postgres://...` が設定されていることを確認する
- 設定済みのはずだが未設定の場合は STATE.md のブロッカーに記録されている通り、ユーザーに Neon プロジェクト設定を依頼する

注意: `drizzle/` 以下のファイルは自動生成のため、手動編集は禁止。
  </action>
  <verify>
    <automated>cd C:/Users/pitan/dev/nafuda && ls drizzle/*.sql 2>/dev/null | xargs grep -l "connections" 2>/dev/null || echo "MIGRATION_FILE_CHECK_NEEDED"</automated>
    <manual>pnpm db:migrate の出力に "All migrations applied" またはエラーなしで完了することを確認</manual>
  </verify>
  <done>drizzle/ ディレクトリに connections テーブルを含むマイグレーションSQLファイルが存在し、pnpm db:migrate が成功している。</done>
</task>

<task type="auto">
  <name>Task 2: /u/$urlId に「つながる」ボタンを追加（isOwnProfile 正しく判定）</name>
  <files>src/routes/u/$urlId.tsx</files>
  <action>
src/routes/u/$urlId.tsx を修正して「つながる」ボタンを追加する。

現在のファイルを読み込んで以下の変更を加える:

1. **セッション確認用サーバー関数を追加**（オプショナルセッション — 公開ページパターン）:
```typescript
import { createServerFn } from '@tanstack/react-start'
import { getRequest } from '@tanstack/react-start/server'
import { auth } from '../../server/auth'

const getOptionalSession = createServerFn({ method: 'GET' })
  .handler(async () => {
    const request = getRequest()
    return await auth.api.getSession({ headers: request.headers })
  })
```

2. **Route loader でセッションも取得し、isOwnProfile をサーバー側で算出する**:

isOwnProfile の判定はサーバー側（loader）で行う。セッションユーザーの urlId を取得して params.urlId と比較する。セッションユーザーに urlId が直接ない場合は、ユーザーIDで Users テーブルから urlId を引く。

```typescript
export const Route = createFileRoute('/u/$urlId')({
  loader: async ({ params }) => {
    const [profile, session] = await Promise.all([
      getPublicProfile({ data: { urlId: params.urlId } }),
      getOptionalSession(),
    ])

    // isOwnProfile: ログインユーザーの urlId とページの urlId を比較
    // session.user が存在し、かつ自分の urlId と一致する場合のみ true
    // auth の User オブジェクトに urlId が含まれない場合は別途 getUserUrlId サーバー関数で取得するか、
    // フォールバックとして false を使い createConnection のサーバー側エラーに依存する
    let isOwnProfile = false
    if (session?.user) {
      // better-auth のセッションユーザーに urlId フィールドがあるか確認する
      // ある場合: isOwnProfile = session.user.urlId === params.urlId
      // ない場合: DB から現在ユーザーの urlId を取得する専用サーバー関数を呼ぶ
      const sessionUser = session.user as { urlId?: string; id: string }
      if (sessionUser.urlId) {
        isOwnProfile = sessionUser.urlId === params.urlId
      } else {
        // urlId が session.user にない場合は getMyUrlId サーバー関数を呼び出す
        // この関数がなければ src/server/functions/user.ts に追加する:
        //   export const getMyUrlId = createServerFn({ method: 'GET' })
        //     .handler(async () => {
        //       const request = getRequest()
        //       const session = await auth.api.getSession({ headers: request.headers })
        //       if (!session) return null
        //       const user = await db.query.users.findFirst({
        //         where: eq(users.id, session.user.id),
        //         columns: { urlId: true },
        //       })
        //       return user?.urlId ?? null
        //     })
        // const myUrlId = await getMyUrlId()
        // isOwnProfile = myUrlId === params.urlId
        //
        // 実装注意: session.user の実際の型を確認してから最適な方法を選択すること。
        // いずれの方法でも isOwnProfile が正しく true/false になることが重要。
        isOwnProfile = false  // 上記のいずれかで置き換える
      }
    }

    return { profile, session, urlId: params.urlId, isOwnProfile }
  },
  component: PublicProfilePage,
})
```

3. **コンポーネントに「つながる」ボタンロジックを追加**:
```typescript
import { useState } from 'react'
import { useNavigate } from '@tanstack/react-router'
import { createConnection } from '../../server/functions/connection'

function PublicProfilePage() {
  const { profile, session, urlId, isOwnProfile } = Route.useLoaderData()
  const navigate = useNavigate()
  const [connected, setConnected] = useState(false)
  const [connecting, setConnecting] = useState(false)
  const [error, setError] = useState<string | null>(null)

  if (!profile) return <div className="p-6 text-sm text-gray-500">プロフィールが見つかりません</div>

  const handleConnect = async () => {
    if (!session?.user) {
      await navigate({ to: '/login' })
      return
    }
    setConnecting(true)
    setError(null)
    try {
      const result = await createConnection({ data: { targetUrlId: urlId } })
      if (result.alreadyConnected) {
        setConnected(true)
      } else {
        setConnected(true)
      }
    } catch (err) {
      const message = err instanceof Error ? err.message : 'エラーが発生しました'
      setError(message)
    } finally {
      setConnecting(false)
    }
  }
```

4. **「つながる」ボタンをJSX末尾（「なふだとは？」リンクの上）に追加**:

自分のプロフィールでは `isOwnProfile === true` の場合はボタンセクション全体を非表示にする。

```tsx
{/* 「つながる」ボタン: isOwnProfile === false の場合のみ表示 */}
{!isOwnProfile && (
  <div className="w-full max-w-sm mt-2">
    {connected ? (
      <div className="w-full text-center px-6 py-3 bg-gray-100 text-gray-500 rounded-xl text-sm font-medium">
        つながり済み ✓
      </div>
    ) : (
      <button
        onClick={handleConnect}
        disabled={connecting}
        className="w-full px-6 py-3 bg-pink-500 text-white rounded-xl text-sm font-medium hover:bg-pink-600 transition-colors disabled:opacity-50"
      >
        {connecting ? 'つながっています...' : 'つながる'}
      </button>
    )}
    {error && <p className="text-xs text-red-500 text-center mt-2">{error}</p>}
  </div>
)}
```

注意:
- isOwnProfile の最終的な実装は session.user の実際の型を確認してから決定すること。session.user に urlId があればそれを使う。なければ DB 照会の追加サーバー関数を実装する。
- isOwnProfile が true の場合はボタン全体を非表示（エラーも表示しない）
- エラー表示は isOwnProfile=false のユーザー向けのみ。サーバー側の「自分自身にはつながれません」エラーは通常 isOwnProfile=true で事前に防ぐが、万が一到達した場合も error state に表示する
- 未ログインユーザーは `navigate({ to: '/login' })` でリダイレクト（ログイン後の戻りは現在スコープ外）
- CONN-02 に従い、ページ閲覧自体ではコネクションを記録しない（ボタンクリックのみ）
  </action>
  <verify>
    <automated>cd C:/Users/pitan/dev/nafuda && pnpm tsc --noEmit 2>&1 | grep -i "urlId" | head -20 || echo "TYPE_CHECK_CLEAN"</automated>
    <manual>grep "isOwnProfile\|createConnection\|つながる" src/routes/u/'$urlId.tsx' を確認。isOwnProfile が false ハードコードになっていないこと（session.user の urlId 比較または getMyUrlId 呼び出しが存在すること）を確認する。</manual>
  </verify>
  <done>src/routes/u/$urlId.tsx に「つながる」ボタンが実装されている。isOwnProfile が loader でセッションユーザーの urlId と params.urlId の比較により正しく算出されている（false ハードコードではない）。自分のプロフィールでは「つながる」ボタンが非表示。未ログイン → ログイン画面リダイレクト。ログイン済み → createConnection 呼び出し。成功後 → 「つながり済み」表示。TypeScriptエラーなし。</done>
</task>

</tasks>

<verification>
```bash
cd C:/Users/pitan/dev/nafuda
# TypeScript型チェック
pnpm tsc --noEmit

# 「つながる」関連コードの存在確認
grep -n "createConnection\|つながる\|isOwnProfile" src/routes/u/'$urlId.tsx'

# isOwnProfile が false ハードコードになっていないことを確認
# （"isOwnProfile = false" という行が最終的な代入でないことを確認）
grep -n "isOwnProfile" src/routes/u/'$urlId.tsx'

# マイグレーションファイルの確認
ls drizzle/*.sql | xargs grep -l "connections" 2>/dev/null && echo "MIGRATION_EXISTS" || echo "MIGRATION_MISSING"
```
</verification>

<success_criteria>
- pnpm db:generate + pnpm db:migrate が成功し、Neon DB に connections テーブルが存在する
- /u/$urlId ページに「つながる」ボタンが存在する（自分以外のプロフィールで表示）
- 自分の /u/$urlId にアクセスした場合は「つながる」ボタンが非表示
- isOwnProfile が loader でセッションユーザーの urlId と params.urlId の比較により算出される（false ハードコードは不可）
- 未ログイン → /login にリダイレクト
- ログイン済み → createConnection が呼ばれ、成功後「つながり済み」に変わる
- エラー時はエラーメッセージを表示する
- pnpm tsc --noEmit がエラーなし
</success_criteria>

<output>
After completion, create `.planning/phases/03-qr-pwa/03-03-SUMMARY.md`
</output>
