---
phase: 02-event-checkin
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/routes/e/$slug.tsx
autonomous: true
requirements:
  - OSHI-04
  - OSHI-05

must_haves:
  truths:
    - "ログインユーザーが /e/$slug でチェックイン済み全参加者の一覧を閲覧できる"
    - "同担拒否フラグを設定したユーザーは参加者一覧に表示されない（SQL フィルタ）"
    - "ログインユーザーは参加者カードから個別プロフィールページに遷移できる"
    - "未ログインユーザーはハンドル名とアバターのみ閲覧でき、プロフィールリンクをクリックできない"
    - "ページに QRコード（このページの URL）が表示される"
    - "ログイン済みなら「参加する」ボタンが表示され、クリックでチェックインできる"
    - "未ログインなら「ログインして参加する」リンクが表示される"
    - "イベントが存在しない場合は 404 メッセージを表示する"
  artifacts:
    - path: "src/routes/e/$slug.tsx"
      provides: "公開イベントページ（QRコード + 参加ボタン + 参加者一覧）"
      exports: ["Route"]
  key_links:
    - from: "src/routes/e/$slug.tsx"
      to: "src/server/functions/event.ts"
      via: "getEventParticipants + checkinToEvent"
      pattern: "getEventParticipants|checkinToEvent"
    - from: "src/routes/e/$slug.tsx"
      to: "qrcode.react"
      via: "QRCodeSVG — useEffect mounted ガード（SSR 非対応のため）"
      pattern: "QRCodeSVG"
    - from: "src/routes/e/$slug.tsx"
      to: "getOptionalSession"
      via: "ローダーで isLoggedIn と defaultPersonaId を取得"
      pattern: "getOptionalSession|isLoggedIn"
---

<objective>
/e/$slug ページを新設計に更新する。QRコード（このページの URL）、「参加する」ボタン、全チェックイン済み参加者一覧（checkedOutAt フィルタなし）を追加する。

Purpose: QR スキャン → 参加ボタン → 一覧確認の新しいチェックインフローを完成させる（OSHI-04, OSHI-05）。
Output: 更新済み /e/$slug.tsx（QRコード + 参加ボタン + 全参加者一覧）
</objective>

<execution_context>
@C:/Users/pitan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pitan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-event-checkin/02-CONTEXT.md
@.planning/phases/02-event-checkin/02-RESEARCH.md
@.planning/phases/02-event-checkin/02-01-SUMMARY.md
@src/routes/e/$slug.tsx
@src/components/ParticipantCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: /e/$slug.tsx を新設計に更新（QRコード + 参加ボタン + 全参加者）</name>
  <files>src/routes/e/$slug.tsx</files>
  <action>
既存の `src/routes/e/$slug.tsx` を新設計に更新する（全面書き換え）。

**追加インポート:**
```typescript
import { QRCodeSVG } from 'qrcode.react'
import { useState, useEffect, useCallback } from 'react'
import { checkinToEvent } from '../../server/functions/event'
import { getOwnProfile } from '../../server/functions/profile'
```

**ローダーの拡張（defaultPersonaId の取得）:**
```typescript
export const Route = createFileRoute('/e/$slug')({
  loader: async ({ params }) => {
    const [data, session] = await Promise.all([
      getEventParticipants({ data: { slug: params.slug } }),
      getOptionalSession(),
    ])
    let defaultPersonaId: string | null = null
    if (session?.user) {
      const profile = await getOwnProfile()
      defaultPersonaId =
        profile?.personas?.find((p) => p.isDefault)?.id
        ?? profile?.personas?.[0]?.id
        ?? null
    }
    return { data, isLoggedIn: !!session?.user, defaultPersonaId }
  },
  component: EventPage,
})
```

**QRCodeDisplay コンポーネント（ファイル内に定義）:**
qrcode.react は SSR 非対応のため mounted state で必ずガードする（RESEARCH.md Pitfall 1, 2 参照）:
```typescript
function QRCodeDisplay({ url }: { url: string }) {
  const [mounted, setMounted] = useState(false)
  useEffect(() => { setMounted(true) }, [])
  if (!mounted) return <div className="w-48 h-48 bg-gray-100 rounded-lg animate-pulse" />
  return (
    <QRCodeSVG
      value={url}
      size={192}
      level="M"
      marginSize={4}
    />
  )
}
```

**EventPage コンポーネント（全面書き換え）:**
```typescript
function EventPage() {
  const { data, isLoggedIn, defaultPersonaId } = Route.useLoaderData()
  const router = useRouter()
  const [isCheckingIn, setIsCheckingIn] = useState(false)
  const [checkinError, setCheckinError] = useState<string | null>(null)
  // currentUrl は useEffect 後のみ取得（window.location.href はブラウザ専用）
  const [currentUrl, setCurrentUrl] = useState('')
  useEffect(() => { setCurrentUrl(window.location.href) }, [])

  const handleCheckin = useCallback(async () => {
    if (!defaultPersonaId || !data) return
    setIsCheckingIn(true)
    setCheckinError(null)
    try {
      await checkinToEvent({ data: { slug: data.event.slug, personaId: defaultPersonaId } })
      await router.invalidate()
    } catch (err) {
      setCheckinError(err instanceof Error ? err.message : 'チェックインに失敗しました')
    } finally {
      setIsCheckingIn(false)
    }
  }, [defaultPersonaId, data, router])

  if (!data) {
    return (
      <div className="min-h-screen flex flex-col items-center justify-center gap-3 p-6 text-center">
        <h1 className="text-2xl font-semibold text-gray-700">イベントが見つかりません</h1>
        <p className="text-sm text-gray-500">URLを確認してください。</p>
        <Link to="/" className="text-sm text-blue-500 underline">トップへ戻る</Link>
      </div>
    )
  }

  return (
    <div className="min-h-screen p-6 max-w-2xl mx-auto flex flex-col gap-6">
      {/* イベント情報ヘッダー */}
      <div className="flex flex-col gap-1">
        <h1 className="text-2xl font-bold text-gray-900">{data.event.name}</h1>
        <p className="text-sm text-gray-500">{data.event.venueName}</p>
        <p className="text-xs text-gray-400">
          {new Date(data.event.eventDate).toLocaleDateString('ja-JP', { year: 'numeric', month: 'long', day: 'numeric' })}
        </p>
      </div>

      {/* QRコード */}
      <div className="flex flex-col items-center gap-2">
        <QRCodeDisplay url={currentUrl} />
        <p className="text-xs text-gray-400">このページのQRコード</p>
      </div>

      {/* 参加ボタン / ログイン誘導 */}
      {isLoggedIn ? (
        defaultPersonaId ? (
          <div className="flex flex-col gap-2">
            <button
              onClick={handleCheckin}
              disabled={isCheckingIn}
              className="w-full py-3 rounded-xl bg-black text-white text-sm font-semibold hover:bg-gray-800 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
            >
              {isCheckingIn ? '参加中...' : '参加する'}
            </button>
            {checkinError && <p className="text-xs text-red-500 text-center">{checkinError}</p>}
          </div>
        ) : (
          <Link
            to="/_protected/profile"
            className="block w-full py-3 rounded-xl border border-black text-center text-sm font-semibold hover:bg-gray-50 transition-colors"
          >
            プロフィールを設定して参加する
          </Link>
        )
      ) : (
        <Link
          to="/login"
          className="block w-full py-3 rounded-xl bg-black text-white text-center text-sm font-semibold hover:bg-gray-800 transition-colors"
        >
          ログインして参加する
        </Link>
      )}

      {/* 参加者カウント */}
      <p className="text-sm text-gray-600 font-medium">
        {data.participants.length}人が参加
      </p>

      {/* 未ログイン時の注意書き（OSHI-05） */}
      {!isLoggedIn && data.participants.length > 0 && (
        <p className="text-sm text-amber-700 bg-amber-50 border border-amber-200 rounded-lg px-4 py-3">
          <Link to="/login" className="font-semibold underline">ログイン</Link>
          するとプロフィールを閲覧できます
        </p>
      )}

      {/* 参加者グリッド */}
      {data.participants.length > 0 ? (
        <div className="grid grid-cols-2 sm:grid-cols-3 gap-3">
          {data.participants.map((p) => (
            <ParticipantCard
              key={p.checkinId}
              displayName={p.displayName}
              avatarUrl={p.avatarUrl}
              // OSHI-05: ログイン済みの場合のみ profileHref を渡す
              profileHref={isLoggedIn ? `/u/${p.urlId}/p/${p.shareToken}` : undefined}
            />
          ))}
        </div>
      ) : (
        <p className="text-sm text-gray-400 text-center py-8">
          まだ参加者はいません
        </p>
      )}
    </div>
  )
}
```

**CRITICAL（RESEARCH.md Pitfall より）:**
- `QRCodeSVG` は必ず `mounted` state で SSR ガードする（`useEffect(() => setMounted(true), [])`）
- `window.location.href` も必ず `useEffect` 後に取得する（サーバーサイドで `window` は undefined）
- `checkinToEvent` の入力は `{ slug, personaId }` のみ（新設計）。eventName/venueName 不要
- `getOptionalSession` は throw redirect せず null を返す（公開ルートのパターン）

**旧設計から削除する要素:**
- `data.participants.length > 0 ? ... : <p>現在チェックイン中の参加者はいません</p>` の「チェックイン中」テキストを「参加者」に変更
- 参加ボタンは新規追加（旧設計には存在しなかった）
- QRコード表示は新規追加
  </action>
  <verify>
    <automated>cd /c/Users/pitan/dev/nafuda &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "e/\\\$slug|e\\\\$slug" || echo "No TypeScript errors in /e/\$slug"</automated>
    <manual>
      1. pnpm dev 起動後、チェックインフォームで作成したイベントの /e/$slug にアクセス
      2. QRコードが表示される（枠のプレースホルダー → SVG QR に切り替わる）
      3. ログイン済みで「参加する」ボタンが表示される
      4. シークレットモードでは「ログインして参加する」リンクが表示される
      5. 「参加する」をクリックするとページがリロードされ自分が参加者一覧に追加される
    </manual>
  </verify>
  <done>TypeScript エラーなし。/e/$slug に QRコード・参加ボタン・参加者一覧が表示され、認証状態に応じてボタン/リンクが切り替わる</done>
</task>

<task type="auto">
  <name>Task 2: 旧コンポーネント削除 + ナビゲーションリンク追加</name>
  <files>
    src/components/EventCheckinCard.tsx
    src/routes/_protected/home.tsx
  </files>
  <action>
**Step 1: EventCheckinCard.tsx を削除する**

`src/components/EventCheckinCard.tsx` はチェックアウト UI 廃止により不要になった。
ファイルを削除する（`rm src/components/EventCheckinCard.tsx` に相当する操作）。

削除前に `src/routes/_protected/events/index.tsx` で EventCheckinCard のインポートがないことを確認する（02-02 で削除済みのはず）。

**Step 2: ホームページの「イベント」ナビゲーションリンク確認・追加**

`src/routes/_protected/home.tsx` を読んで、`/events` へのリンクが存在するか確認する。
- 存在する場合: 変更不要
- 存在しない場合: 既存のナビゲーションセクション（他のリンクが並んでいる場所）に `/events` リンクを追加する

リンクのラベルは「イベント」、遷移先は `/_protected/events/` とする（TanStack Router の Link コンポーネント使用）。

**注意:** home.tsx の詳細な構造は実際のファイルを読んでから変更すること。
  </action>
  <verify>
    <automated>cd /c/Users/pitan/dev/nafuda &amp;&amp; ls src/components/EventCheckinCard.tsx 2>/dev/null &amp;&amp; echo "FILE STILL EXISTS - DELETE IT" || echo "Deleted OK" &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -c "error TS" || echo "0"</automated>
    <manual>EventCheckinCard.tsx が存在しないこと。ホームページに「イベント」リンクが表示されること（または既に存在すること）。TypeScript エラーなし。</manual>
  </verify>
  <done>EventCheckinCard.tsx が削除され、TypeScript エラーなし。ホームページからイベントページへのナビゲーションが確認できる</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` でエラーなし
2. /e/$slug が認証なしでアクセス可能（公開ルート）
3. QRCodeSVG が mounted state ガードありで実装されている（SSR 安全）
4. window.location.href が useEffect 後のみ参照されている
5. isLoggedIn=false のとき ParticipantCard に profileHref が渡されない
6. getEventParticipants の WHERE 句に isNull(checkedOutAt) が存在しない（全参加者を返す）
7. EventCheckinCard.tsx が src/components/ に存在しない
</verification>

<success_criteria>
- /e/$slug に QRコード・参加ボタン・全参加者一覧（checkedOutAt フィルタなし）が表示される
- ログイン済みユーザーは「参加する」ボタンをクリックして一覧に追加される
- 未ログインユーザーはハンドル名・アバターのみ閲覧でき、プロフィールリンクが無効
- dojinReject=true のユーザーは一覧に表示されない（SQL フィルタ）
- QRコードが SSR クラッシュなくレンダリングされる
- EventCheckinCard.tsx が削除済み
- TypeScript エラーなし
</success_criteria>

<output>
完了後、`.planning/phases/02-event-checkin/02-03-SUMMARY.md` を作成する。
</output>
