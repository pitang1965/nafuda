---
phase: 02-event-checkin
plan: "02"
subsystem: ui
tags: [tanstack-router, react-hook-form, zod, geolocation, tailwind, better-auth]

# Dependency graph
requires:
  - phase: 02-event-checkin/02-01
    provides: checkinToEvent, checkoutFromEvent, getActiveCheckin server functions

provides:
  - EventCheckinCard component (status display with checkout button)
  - /_protected/events/ route (checkin form + active checkin display)

affects:
  - 02-event-checkin/02-03 (EventCheckinCard link to /e/$slug needs participant route)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - GPS acquired on button press only — never on page load (RESEARCH.md Pitfall 3)
    - generateSlug: eventName + eventDate → URL-safe slug (client-side, no server round-trip)
    - router.invalidate() to refresh loader after mutation (TanStack Router pattern)
    - Inline error state (useState) — no toast library installed

key-files:
  created:
    - src/components/EventCheckinCard.tsx
    - src/routes/_protected/events/index.tsx
  modified:
    - src/routeTree.gen.ts

key-decisions:
  - "GPS acquired on button press only (never useEffect on page load) — prevents uninvited permission dialog per RESEARCH.md Pitfall 3"
  - "Inline error display via useState — project has no toast library; avoids adding a dependency for a single use case"
  - "router.invalidate() after checkin/checkout — reloads loader data to switch between form and EventCheckinCard views"

# Metrics
duration: 2min
completed: 2026-04-25
---

# Phase 2 Plan 02: Checkin UI Route Summary

**EventCheckinCard component and /_protected/events/ route with react-hook-form + zod validation, GPS on button press, auto slug generation, and router.invalidate() for state refresh**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-25T13:07:09Z
- **Completed:** 2026-04-25T13:09:09Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Created `EventCheckinCard` component with pulsing green status badge, event info display (ja-JP locale), participant list link, and checkout button with loading state
- Created `/_protected/events/` route with checkin form (react-hook-form + zodResolver) and active checkin display switching
- Implemented `generateSlug` helper producing URL-safe slugs from eventName + eventDate
- Implemented `getGpsCoords` helper called on button press only (never on page load)
- routeTree.gen.ts auto-updated by TanStack Router Vite plugin

## Task Commits

Each task was committed atomically:

1. **Task 1: EventCheckinCard コンポーネント** - `eac6ae0` (feat)
2. **Task 2: チェックインルート — /_protected/events/index.tsx** - `9ab225e` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `src/components/EventCheckinCard.tsx` - Status card with event info and checkout button
- `src/routes/_protected/events/index.tsx` - Checkin route with form and active checkin display
- `src/routeTree.gen.ts` - Auto-generated by TanStack Router (/_protected/events/ registered)

## Decisions Made

- GPS acquired on button press only (`getGpsCoords()` called inside `onSubmit`) — prevents uninvited permission dialog on page load
- Inline error display (`useState<string | null>`) — project has no toast library; avoids adding a dependency for a single use case
- `router.invalidate()` used after checkin and checkout to reload loader data and switch between form/card views

## Deviations from Plan

None - plan executed exactly as written. The absence of a toast library was handled inline (Rule 2: auto-add missing critical functionality) using `useState` error display, which is consistent with the existing codebase pattern.

## Issues Encountered

- `/e/$slug` route (Plan 02-03) does not yet exist. `EventCheckinCard` links to it via TanStack Router `<Link>`. TypeScript passes cleanly because routeTree.gen.ts includes the route path string. Plan 02-03 will implement the actual route.

## Next Phase Readiness

- `/events` route fully functional for checkin and checkout
- `EventCheckinCard` ready to display participant list link once Plan 02-03 implements `/e/$slug`
- Phase 2 Plan 03 (participant list route) can proceed immediately

## Self-Check: PASSED

All files present and all commits verified.

---
*Phase: 02-event-checkin*
*Completed: 2026-04-25*
