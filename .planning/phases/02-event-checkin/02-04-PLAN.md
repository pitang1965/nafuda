---
phase: 02-event-checkin
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
files_modified: []
autonomous: false
requirements:
  - OSHI-03
  - OSHI-04
  - OSHI-05

must_haves:
  truths:
    - "主催者がイベントを作成し QR を共有できる"
    - "参加者が QR スキャン → /e/$slug にアクセスして「参加する」をクリックできる"
    - "参加後に自分が参加者一覧に表示される"
    - "未ログインユーザーはハンドル名・アバターのみ閲覧でき、プロフィールリンクが無効"
  artifacts:
    - path: "src/routes/_protected/events/index.tsx"
      provides: "イベント一覧（主催・参加）"
    - path: "src/routes/_protected/events/new.tsx"
      provides: "イベント作成フォーム"
    - path: "src/routes/e/$slug.tsx"
      provides: "公開イベントページ（QR + 参加ボタン + 一覧）"
  key_links:
    - from: "イベント作成フロー"
      to: "参加フロー"
      via: "/e/$slug QRコードの URL が参加ボタンに接続されている"
      pattern: "checkinToEvent"
---

<objective>
Phase 2 全機能（イベント作成 → QR 共有 → チェックイン → 参加者一覧）のエンドツーエンド動作を人手で確認する。

Purpose: OSHI-03, OSHI-04, OSHI-05 が正しく動作することを実際のユーザーフローで検証し、Phase 2 を完了とする。
Output: 承認済みフロー確認、Phase 2 完了
</objective>

<execution_context>
@C:/Users/pitan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pitan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-event-checkin/02-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Phase 2 全フローのエンドツーエンド確認</name>
  <files>N/A</files>
  <action>
Phase 2 全機能（イベント作成 → QR 共有 → チェックイン → 参加者一覧）のエンドツーエンド動作を人手で確認する。
`pnpm dev` でアプリを起動し、以下の確認手順を実施する。
  </action>
  <verify>
    <automated>echo "Human verification required — see how-to-verify below"</automated>
    <manual>
以下の手順を順番に確認してください:

**フロー A: イベント作成 → QR 表示**
1. ログインして /events にアクセス
2. 「新規イベントを作成」ボタンをクリックして /events/new に遷移
3. イベント名・会場名・日付を入力して「イベントを作成する」ボタンをクリック
4. /e/$slug にリダイレクトされること
5. QRコード（SVG）が表示されること（灰色プレースホルダーが QR 画像に切り替わる）

**フロー B: 参加フロー**
6. /e/$slug ページで「参加する」ボタンが表示されていること
7. 「参加する」をクリックするとページがリロードされ、自分のハンドル名・アバターが参加者一覧に追加されること
8. /events に戻り、「参加したイベント」セクションに追加されたイベントが表示されること

**フロー C: 未ログインアクセス（OSHI-05）**
9. シークレット / プライベートブラウザで同じ /e/$slug を開く
10. 参加者のハンドル名・アバターは表示されること
11. 「ログインして参加する」リンクが表示されること
12. 参加者カードがクリックできない（プロフィールリンクが無効）こと

**フロー D: 一覧確認（OSHI-04）**
13. ログイン状態で /e/$slug にアクセスして参加者カードをクリックするとプロフィールページに遷移できること
    </manual>
    <sampling_rate>このチェックポイントは Wave 3 — 02-02 と 02-03 の完了後に実行する</sampling_rate>
  </verify>
  <done>全手順が期待通りに動作し、ユーザーが "approved" を入力した</done>
</task>

</tasks>

<verification>
ユーザーが "approved" を入力した場合、Phase 2 は完了。
問題が報告された場合は該当プランを修正して再実行する。
</verification>

<success_criteria>
- OSHI-03: イベント作成（/events/new）→ /e/$slug リダイレクトが動作する
- OSHI-04: ログインユーザーが /e/$slug で参加者一覧（プロフィールリンク付き）を閲覧できる
- OSHI-05: 未ログインユーザーが /e/$slug でハンドル名・アバターのみ閲覧でき、プロフィールリンクが無効
- 人的確認（checkpoint:human-verify）が "approved" で完了する
</success_criteria>

<output>
完了後、`.planning/phases/02-event-checkin/02-04-SUMMARY.md` を作成する。
</output>
