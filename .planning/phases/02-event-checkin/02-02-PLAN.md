---
phase: 02-event-checkin
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/routes/_protected/events/index.tsx
  - src/components/EventCheckinCard.tsx
autonomous: true
requirements:
  - OSHI-03

must_haves:
  truths:
    - "ログインユーザーが /events でイベント名・会場名・日付を入力してチェックインできる"
    - "チェックイン時にブラウザが GPS 位置情報を取得し（拒否しても続行可能）座標が保存される"
    - "チェックイン中ステータスが画面に表示され、イベント名・会場名が確認できる"
    - "チェックアウトボタンを押すとチェックイン中ステータスが解除される"
    - "slug がイベント名+日付から自動生成される（ユーザー入力不要）"
  artifacts:
    - path: "src/routes/_protected/events/index.tsx"
      provides: "チェックインUI（認証必須ルート）"
      exports: ["Route"]
    - path: "src/components/EventCheckinCard.tsx"
      provides: "チェックイン中ステータス表示カード"
      exports: ["EventCheckinCard"]
  key_links:
    - from: "src/routes/_protected/events/index.tsx"
      to: "src/server/functions/event.ts"
      via: "checkinToEvent, checkoutFromEvent, getActiveCheckin"
      pattern: "checkinToEvent|checkoutFromEvent|getActiveCheckin"
    - from: "src/routes/_protected/events/index.tsx"
      to: "navigator.geolocation"
      via: "getGpsCoords() helper function"
      pattern: "navigator\\.geolocation"
---

<objective>
ログインユーザーがイベントにチェックインできる保護ルートを実装する。

Purpose: OSHI-03「ユーザーはイベントにチェックインできる（日付・会場名・イベント名・GPS座標を記録）」を満たすUI。
Output: /_protected/events/index.tsx（フォーム+ステータス）、EventCheckinCard コンポーネント
</objective>

<execution_context>
@C:/Users/pitan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pitan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-event-checkin/02-RESEARCH.md
@.planning/phases/02-event-checkin/02-01-SUMMARY.md
@src/routes/_protected/home.tsx
@src/server/functions/event.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: EventCheckinCard コンポーネント</name>
  <files>src/components/EventCheckinCard.tsx</files>
  <action>
チェックイン中ステータスを表示するカードコンポーネントを作成する。

**Props:**
```typescript
interface EventCheckinCardProps {
  eventName: string
  venueName: string
  eventDate: string        // ISO日付文字列
  checkedInAt: string      // ISO日付文字列
  eventSlug: string        // /e/$slug リンク用
  onCheckout: () => void
  isCheckingOut?: boolean
}
```

**表示内容:**
- イベント名（見出し）
- 会場名
- イベント日付（ja-JP locale で日本語表示: `new Date(eventDate).toLocaleDateString('ja-JP')`)
- チェックイン時刻（`new Date(checkedInAt).toLocaleTimeString('ja-JP', { hour: '2-digit', minute: '2-digit' })`)
- 「イベント参加者を見る」リンク → `/e/${eventSlug}` (TanStack Router の Link コンポーネント使用)
- 「チェックアウト」ボタン（onCheckout 呼び出し、isCheckingOut 中は disabled）

**スタイリング:** Tailwind v4 使用。Phase 1 のカードスタイル（`rounded-xl border bg-card`等）に準拠。

**注意:** import type を使用（verbatimModuleSyntax 対策、Phase 1 locked decision）。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep "EventCheckinCard" || echo "No TypeScript errors in EventCheckinCard"</automated>
    <manual>EventCheckinCard.tsx が存在し、Props の全フィールドが表示されていることを確認</manual>
  </verify>
  <done>TypeScript エラーなし。EventCheckinCard が export されており、eventName・venueName・チェックアウトボタンが含まれる。</done>
</task>

<task type="auto">
  <name>Task 2: チェックインルート — /_protected/events/index.tsx</name>
  <files>src/routes/_protected/events/index.tsx</files>
  <action>
`src/routes/_protected/events/index.tsx` を新規作成する。`_protected` 配下なので Better Auth セッションチェックは親レイアウトが処理済み。

**ルート定義:**
```typescript
export const Route = createFileRoute('/_protected/events/')({
  loader: async () => {
    // ローダーでデフォルトペルソナIDを取得してアクティブチェックインを確認
    // getActiveCheckin は personaId が必要なため、先に getOwnProfile を呼ぶ
    const profile = await getOwnProfile()
    const defaultPersona = profile?.personas?.find(p => p.isDefault) ?? profile?.personas?.[0]
    if (!defaultPersona) return { activeCheckin: null, personaId: null }
    const activeCheckin = await getActiveCheckin({ data: { personaId: defaultPersona.id } })
    return { activeCheckin, personaId: defaultPersona.id }
  },
  component: EventsPage,
})
```

**フォームスキーマ (zod):**
```typescript
const checkinSchema = z.object({
  eventName: z.string().min(1, 'イベント名を入力してください').max(100),
  venueName: z.string().min(1, '会場名を入力してください').max(100),
  eventDate: z.string().min(1, '日付を選択してください'),
})
```

**GPS 取得ヘルパー（コンポーネント外に定義）:**
```typescript
async function getGpsCoords(): Promise<{ x: number; y: number } | null> {
  return new Promise((resolve) => {
    if (!navigator.geolocation) { resolve(null); return }
    navigator.geolocation.getCurrentPosition(
      (pos) => resolve({ x: pos.coords.longitude, y: pos.coords.latitude }),
      () => resolve(null),
      { timeout: 5000, enableHighAccuracy: false }
    )
  })
}
```

**slug 自動生成ヘルパー:**
```typescript
function generateSlug(eventName: string, eventDate: string): string {
  const datePart = eventDate.replace(/-/g, '')  // "2026-04-05" → "20260405"
  const namePart = eventName
    .toLowerCase()
    .replace(/[^\w\s-]/g, '')  // 特殊文字除去
    .replace(/[\s_]+/g, '-')   // スペースをハイフンに
    .replace(/-+/g, '-')       // 連続ハイフン除去
    .slice(0, 50)
  return `${namePart}-${datePart}`
}
```

**コンポーネント実装:**
- `useLoaderData()` でアクティブチェックインと personaId を取得
- アクティブチェックインがある場合: EventCheckinCard を表示（チェックアウトボタン付き）
- アクティブチェックインがない場合: チェックインフォームを表示

**フォーム（react-hook-form + zodResolver）:**
- フィールド: eventName（text input）、venueName（text input）、eventDate（date input）
- submit ハンドラ:
  1. GPS 取得（「チェックイン」ボタン押下時のみ — ページロード時は呼ばない）
  2. slug = generateSlug(eventName, eventDate)
  3. `await checkinToEvent({ data: { slug, eventName, venueName, eventDate, personaId, gpsCoordinates: gps ?? undefined } })`
  4. 成功後: `router.invalidate()` でローダーを再実行してアクティブチェックイン表示に切り替え
- エラーは toast（既存パターン）で表示

**チェックアウト処理:**
- EventCheckinCard の onCheckout: `await checkoutFromEvent({ data: { checkinId } })`
- 成功後: `router.invalidate()`

**注意:**
- GPS 取得は必ずボタン押下時（ページロード時の `useEffect` 呼び出し禁止 — RESEARCH.md Pitfall 3）
- date input の value 形式は `YYYY-MM-DD`（input type="date" のネイティブ形式）
- checkinToEvent に渡す eventDate は ISO datetime 文字列に変換: `new Date(formData.eventDate).toISOString()`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep "events" || echo "No TypeScript errors in events route"</automated>
    <manual>
      1. pnpm dev を起動してログイン状態で /events にアクセス
      2. フォームが表示されイベント名・会場名・日付を入力できること
      3. 「チェックイン」ボタン押下でブラウザの位置情報ダイアログが表示されること（または拒否後も続行できること）
      4. チェックイン後に EventCheckinCard が表示されイベント名・チェックアウトボタンが見えること
    </manual>
  </verify>
  <done>TypeScript エラーなし。/events ルートが表示され、チェックインフォームとアクティブチェックイン時のカード表示が動作する。GPS 取得はボタン押下時のみ実行される。</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` で TypeScript エラーなし
2. `pnpm dev` 起動後、ログイン状態で `/events` にアクセスしてフォームが表示される
3. チェックインフォームに eventName・venueName・eventDate 入力欄がある
4. チェックイン後に EventCheckinCard が表示される（イベント名・チェックアウトボタン）
5. GPS ダイアログはボタン押下時のみ（ページロード時は表示されない）
</verification>

<success_criteria>
- /_protected/events/index.tsx が TanStack Router によりルートとして認識される
- チェックインフォームから checkinToEvent が正常に呼び出せる
- アクティブチェックイン時は EventCheckinCard が表示され、チェックアウトできる
- GPS は任意（拒否しても）チェックインが完了する
</success_criteria>

<output>
After completion, create `.planning/phases/02-event-checkin/02-02-SUMMARY.md`
</output>
