---
phase: 02-event-checkin
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/routes/_protected/events/index.tsx
  - src/routes/_protected/events/new.tsx
autonomous: true
requirements:
  - OSHI-03
  - OSHI-04

must_haves:
  truths:
    - "ログインユーザーが /events でイベント一覧（主催・参加の2セクション）を閲覧できる"
    - "「新規イベントを作成」ボタンから /events/new に遷移できる"
    - "イベント名・会場名・日付を入力して送信すると /e/$slug にリダイレクトされる"
    - "イベントが存在しない場合のみ新規作成され、slug 衝突時は既存イベントページに遷移する"
  artifacts:
    - path: "src/routes/_protected/events/index.tsx"
      provides: "イベント一覧ページ（主催・参加の2セクション）"
      exports: ["Route"]
    - path: "src/routes/_protected/events/new.tsx"
      provides: "イベント作成フォーム"
      exports: ["Route"]
  key_links:
    - from: "src/routes/_protected/events/index.tsx"
      to: "src/server/functions/event.ts"
      via: "getMyEvents()"
      pattern: "getMyEvents"
    - from: "src/routes/_protected/events/new.tsx"
      to: "src/server/functions/event.ts"
      via: "createEvent() → router.navigate('/e/$slug')"
      pattern: "createEvent.*navigate"
---

<objective>
イベント一覧ページ（/events）とイベント作成フォーム（/events/new）を実装する。

Purpose: ログインユーザーが自分の主催・参加イベントを一覧で確認でき、新規イベントを作成して QR 共有フローの起点を作れるようにする。
Output: 書き換え済み events/index.tsx（一覧）、新規 events/new.tsx（作成フォーム）
</objective>

<execution_context>
@C:/Users/pitan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pitan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-event-checkin/02-CONTEXT.md
@.planning/phases/02-event-checkin/02-RESEARCH.md
@.planning/phases/02-event-checkin/02-01-SUMMARY.md
@src/routes/_protected/home.tsx
@src/server/functions/event.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: /events を一覧ページに書き換え（旧チェックインフォームを完全置換）</name>
  <files>src/routes/_protected/events/index.tsx</files>
  <action>
`src/routes/_protected/events/index.tsx` を全面書き換えする（旧チェックインフォームの全ロジックを削除）。

**インポート:**
```typescript
import { createFileRoute, Link } from '@tanstack/react-router'
import { getMyEvents } from '../../../server/functions/event'
```
`checkoutFromEvent`, `getActiveCheckin`, `EventCheckinCard`, `useRouter`, `useState`, `useForm`, `zodResolver`, `z`, `getOwnProfile` はすべて削除する。

**ルート定義:**
```typescript
export const Route = createFileRoute('/_protected/events/')({
  loader: async () => {
    return await getMyEvents()
  },
  component: EventsPage,
})
```

**コンポーネント:**
```typescript
function EventsPage() {
  const { hosted, participated } = Route.useLoaderData()

  return (
    <div className="min-h-screen flex flex-col">
      <div className="p-4 border-b flex items-center justify-between">
        <h1 className="text-lg font-bold">イベント</h1>
        <Link
          to="/_protected/events/new"
          className="text-sm font-medium px-4 py-2 bg-black text-white rounded-lg hover:bg-gray-800 transition-colors"
        >
          新規イベントを作成
        </Link>
      </div>

      <div className="flex-1 p-6 flex flex-col gap-8 max-w-lg mx-auto w-full">
        {/* 主催したイベント */}
        <section className="flex flex-col gap-3">
          <h2 className="text-base font-semibold text-gray-700">主催したイベント</h2>
          {hosted.length === 0 ? (
            <p className="text-sm text-gray-400">まだ作成したイベントはありません</p>
          ) : (
            hosted.map(event => <EventListItem key={event.id} event={event} />)
          )}
        </section>

        {/* 参加したイベント */}
        <section className="flex flex-col gap-3">
          <h2 className="text-base font-semibold text-gray-700">参加したイベント</h2>
          {participated.length === 0 ? (
            <p className="text-sm text-gray-400">まだ参加したイベントはありません</p>
          ) : (
            participated.map(event => <EventListItem key={event.id} event={event} />)
          )}
        </section>
      </div>
    </div>
  )
}
```

**EventListItem コンポーネント（同ファイル内に定義）:**
```typescript
function EventListItem({ event }: { event: { id: string; slug: string; name: string; venueName: string; eventDate: Date | string } }) {
  return (
    <Link
      to="/e/$slug"
      params={{ slug: event.slug }}
      className="block rounded-xl border bg-card p-4 shadow-sm hover:shadow-md transition-shadow"
    >
      <p className="font-semibold text-gray-900 text-sm">{event.name}</p>
      <p className="text-xs text-gray-500 mt-0.5">{event.venueName}</p>
      <p className="text-xs text-gray-400 mt-1">
        {new Date(event.eventDate).toLocaleDateString('ja-JP', { year: 'numeric', month: 'long', day: 'numeric' })}
      </p>
    </Link>
  )
}
```

**注意:** `EventCheckinCard` コンポーネントはこのルートから参照されなくなるが、ファイル自体の削除は次のプランで行う（今回は参照を外すのみ）。
  </action>
  <verify>
    <automated>cd /c/Users/pitan/dev/nafuda &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep "events/index" || echo "No TypeScript errors in events/index"</automated>
    <manual>ブラウザで /events にアクセスし、「主催したイベント」「参加したイベント」の2セクションと「新規イベントを作成」ボタンが表示されること</manual>
  </verify>
  <done>TypeScript エラーなし。/events が一覧ページとして表示され、2 セクションが存在し、新規作成ボタンが /events/new にリンクされている</done>
</task>

<task type="auto">
  <name>Task 2: /events/new — イベント作成フォームを新規作成</name>
  <files>src/routes/_protected/events/new.tsx</files>
  <action>
`src/routes/_protected/events/new.tsx` を新規作成する。

**インポート:**
```typescript
import { createFileRoute, useRouter } from '@tanstack/react-router'
import { useState } from 'react'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import { z } from 'zod'
import { createEvent } from '../../../server/functions/event'
```

**フォームスキーマ:**
```typescript
const newEventSchema = z.object({
  eventName: z.string().min(1, 'イベント名を入力してください').max(100),
  venueName: z.string().min(1, '会場名を入力してください').max(100),
  eventDate: z.string().min(1, '日付を選択してください'),
})
type NewEventFormValues = z.infer<typeof newEventSchema>
```

**ルート定義:**
```typescript
export const Route = createFileRoute('/_protected/events/new')({
  component: NewEventPage,
})
```

**コンポーネント:**
```typescript
function NewEventPage() {
  const router = useRouter()
  const [submitError, setSubmitError] = useState<string | null>(null)
  const { register, handleSubmit, formState: { errors, isSubmitting } } = useForm<NewEventFormValues>({
    resolver: zodResolver(newEventSchema),
  })

  const onSubmit = async (formData: NewEventFormValues) => {
    setSubmitError(null)
    try {
      const event = await createEvent({
        data: {
          eventName: formData.eventName,
          venueName: formData.venueName,
          eventDate: new Date(formData.eventDate).toISOString(),
        }
      })
      await router.navigate({ to: '/e/$slug', params: { slug: event.slug } })
    } catch (err) {
      setSubmitError(err instanceof Error ? err.message : 'イベントの作成に失敗しました')
    }
  }

  return (
    <div className="min-h-screen flex flex-col">
      <div className="p-4 border-b flex items-center gap-3">
        <button
          onClick={() => router.history.back()}
          className="text-sm text-gray-500 hover:text-gray-800"
        >
          ← 戻る
        </button>
        <h1 className="text-lg font-bold">新規イベント作成</h1>
      </div>

      <form onSubmit={handleSubmit(onSubmit)} className="flex-1 p-6 flex flex-col gap-4 max-w-lg mx-auto w-full">
        <div className="rounded-xl border bg-card p-5 shadow-sm flex flex-col gap-4">

          {/* イベント名 */}
          <div className="flex flex-col gap-1">
            <label className="text-sm font-medium" htmlFor="eventName">イベント名</label>
            <input
              id="eventName"
              type="text"
              placeholder="例: コミックマーケット105"
              className="border rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-black"
              {...register('eventName')}
            />
            {errors.eventName && <p className="text-xs text-red-500">{errors.eventName.message}</p>}
          </div>

          {/* 会場名 */}
          <div className="flex flex-col gap-1">
            <label className="text-sm font-medium" htmlFor="venueName">会場名</label>
            <input
              id="venueName"
              type="text"
              placeholder="例: 東京ビッグサイト"
              className="border rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-black"
              {...register('venueName')}
            />
            {errors.venueName && <p className="text-xs text-red-500">{errors.venueName.message}</p>}
          </div>

          {/* 日付 */}
          <div className="flex flex-col gap-1">
            <label className="text-sm font-medium" htmlFor="eventDate">イベント日付</label>
            <input
              id="eventDate"
              type="date"
              className="border rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-black"
              {...register('eventDate')}
            />
            {errors.eventDate && <p className="text-xs text-red-500">{errors.eventDate.message}</p>}
          </div>
        </div>

        {submitError && <p className="text-sm text-red-600 px-1">{submitError}</p>}

        <button
          type="submit"
          disabled={isSubmitting}
          className="w-full py-3 rounded-xl bg-black text-white text-sm font-semibold hover:bg-gray-800 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
        >
          {isSubmitting ? '作成中...' : 'イベントを作成する'}
        </button>
      </form>
    </div>
  )
}
```

**注意:**
- `new.tsx` のルートパスが `/_protected/events/new` になるよう `createFileRoute` に指定する
- リダイレクト後は `/e/$slug` ページが表示される（QRコード+参加ボタンは 02-03 で追加）
  </action>
  <verify>
    <automated>cd /c/Users/pitan/dev/nafuda &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep "events/new" || echo "No TypeScript errors in events/new"</automated>
    <manual>ブラウザで /events/new にアクセスし、イベント名・会場名・日付の3入力欄と「イベントを作成する」ボタンが表示され、送信後に /e/$slug にリダイレクトされること</manual>
  </verify>
  <done>TypeScript エラーなし。/events/new でフォームが表示され、送信後に /e/[生成slug] にリダイレクトされる</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` で TypeScript エラーなし
2. `pnpm dev` 起動後、ログイン状態で `/events` にアクセスして「主催したイベント」「参加したイベント」2セクションが表示される
3. 「新規イベントを作成」ボタンが `/events/new` に遷移する
4. `/events/new` でフォームを記入して送信すると `/e/$slug` にリダイレクトされる
5. 同じイベント名+日付で再送信しても同じ slug のページにリダイレクトされる（重複作成なし）
</verification>

<success_criteria>
- /_protected/events/index.tsx が一覧ページとして動作する（旧チェックインフォームは削除済み）
- /_protected/events/new.tsx が作成フォームとして動作する
- createEvent → /e/$slug リダイレクトが正常動作する
- TypeScript エラーなし
</success_criteria>

<output>
完了後、`.planning/phases/02-event-checkin/02-02-SUMMARY.md` を作成する。
</output>
