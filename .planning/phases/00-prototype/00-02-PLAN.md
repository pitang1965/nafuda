---
phase: 00-prototype
plan: "02"
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - src/pages/ProfilePage.tsx
  - src/pages/EventRoomPage.tsx
  - src/pages/LoginMockPage.tsx
  - src/components/profile/ProfileCard.tsx
  - src/components/profile/SnsLinkList.tsx
  - src/components/event/ParticipantCard.tsx
  - src/components/event/ParticipantList.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "ユーザーはモックプロフィールカードを閲覧でき、ハンドル名・アバター・推しタグ・SNSリンクが表示される"
    - "ユーザーはモックのイベントルーム画面で参加者カードがアニメーション付きで出現するのを確認できる"
    - "ユーザーはモックログインボタンをタップするとログイン状態をシミュレートできる（DB・OAuth不使用）"
    - "/p/:userId にアクセスするとモックプロフィールが表示される"
    - "/event/:eventId にアクセスするとイベントルームが表示される"
  artifacts:
    - path: "src/pages/ProfilePage.tsx"
      provides: "プロフィール表示ページ（/p/:userId ルート）"
      contains: "MOCK_PROFILE"
    - path: "src/pages/EventRoomPage.tsx"
      provides: "イベントルームページ（/event/:eventId ルート）"
      contains: "MOCK_EVENT"
    - path: "src/components/profile/ProfileCard.tsx"
      provides: "ハンドル名・アバター・推しタグ・SNSリンクを表示するカード"
      exports: ["ProfileCard"]
    - path: "src/components/event/ParticipantCard.tsx"
      provides: "motion アニメーション付き参加者カード"
      contains: "motion.div"
    - path: "src/pages/LoginMockPage.tsx"
      provides: "モックログイン画面（useState のみ、DB・OAuth なし）"
      contains: "setIsLoggedIn"
  key_links:
    - from: "src/pages/ProfilePage.tsx"
      to: "src/data/mockData.ts"
      via: "MOCK_PROFILE import"
      pattern: "MOCK_PROFILE"
    - from: "src/components/event/ParticipantCard.tsx"
      to: "motion/react"
      via: "motion.div アニメーション"
      pattern: "motion\\.div"
    - from: "src/pages/EventRoomPage.tsx"
      to: "src/components/event/ParticipantList.tsx"
      via: "MOCK_EVENT.participants 配列を渡す"
      pattern: "participants"
---

<objective>
モックプロフィールカード画面・イベントルーム画面・モックログイン画面を実装する。DB・OAuth は使わず、mockData.ts のデータのみで全画面を動作させる。

Purpose: コアUX仮説（プロフィール表示・イベントルーム参加者出現・モックログイン）を画面として検証できる状態にする。
Output: ProfilePage・EventRoomPage・LoginMockPage の完全実装、ProfileCard・ParticipantCard コンポーネント
</objective>

<execution_context>
@C:/Users/pitan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pitan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-prototype/00-RESEARCH.md
@.planning/phases/00-prototype/00-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: プロフィールカードコンポーネントと ProfilePage の実装</name>
  <files>
    src/components/profile/ProfileCard.tsx
    src/components/profile/SnsLinkList.tsx
    src/pages/ProfilePage.tsx
    src/pages/LoginMockPage.tsx
  </files>
  <action>
1. `src/components/profile/SnsLinkList.tsx` を新規作成する:
   ```tsx
   import { SnsLink } from '../../data/mockData'

   const PLATFORM_LABELS: Record<SnsLink['platform'], string> = {
     x: 'X',
     instagram: 'Instagram',
     discord: 'Discord',
     line_openchat: 'LINE オープンチャット',
     tiktok: 'TikTok',
     youtube: 'YouTube',
   }

   const PLATFORM_COLORS: Record<SnsLink['platform'], string> = {
     x: 'bg-black text-white',
     instagram: 'bg-pink-500 text-white',
     discord: 'bg-indigo-500 text-white',
     line_openchat: 'bg-green-500 text-white',
     tiktok: 'bg-gray-900 text-white',
     youtube: 'bg-red-600 text-white',
   }

   interface SnsLinkListProps {
     links: SnsLink[]
   }

   export function SnsLinkList({ links }: SnsLinkListProps) {
     if (links.length === 0) return null
     return (
       <div className="flex flex-col gap-2">
         {links.map((link) => (
           <a
             key={link.platform}
             href={link.url}
             target="_blank"
             rel="noopener noreferrer"
             className={`flex items-center gap-2 px-4 py-2 rounded-full text-sm font-medium w-full ${PLATFORM_COLORS[link.platform]}`}
           >
             <span className="font-semibold">{PLATFORM_LABELS[link.platform]}</span>
             <span className="opacity-80">{link.handle}</span>
           </a>
         ))}
       </div>
     )
   }
   ```

2. `src/components/profile/ProfileCard.tsx` を新規作成する:
   ```tsx
   import { MockProfile } from '../../data/mockData'
   import { SnsLinkList } from './SnsLinkList'

   interface ProfileCardProps {
     profile: MockProfile
     onQROpen?: () => void
   }

   export function ProfileCard({ profile, onQROpen }: ProfileCardProps) {
     return (
       <div className="bg-white rounded-2xl shadow-md p-6 flex flex-col items-center gap-4 max-w-sm w-full mx-auto">
         {/* アバター */}
         <img
           src={profile.avatarUrl}
           alt={profile.handle}
           className="w-24 h-24 rounded-full border-4 border-pink-200"
         />

         {/* ハンドル名 */}
         <h1 className="text-xl font-bold text-gray-900">{profile.handle}</h1>

         {/* 推しタグ */}
         {profile.oshiTags.length > 0 && (
           <div className="flex flex-wrap gap-1 justify-center">
             {profile.oshiTags.map((tag) => (
               <span
                 key={tag}
                 className="text-xs text-pink-600 bg-pink-50 px-3 py-1 rounded-full font-medium"
               >
                 {tag}
               </span>
             ))}
           </div>
         )}

         {/* SNSリンク */}
         <div className="w-full">
           <SnsLinkList links={profile.snsLinks} />
         </div>

         {/* QRボタン */}
         {onQROpen && (
           <button
             onClick={onQROpen}
             className="mt-2 flex items-center gap-2 px-6 py-2 bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-full text-sm font-medium transition-colors"
           >
             <span>QRコードを表示</span>
           </button>
         )}
       </div>
     )
   }
   ```

3. `src/pages/ProfilePage.tsx` を完全実装で置き換える。URL パラメータ `:userId` を使い、`mock-user-001` または未指定の場合は MOCK_PROFILE を表示する（QRBottomSheet は 00-03 プランで追加するため、今は onQROpen を未接続にする）:
   ```tsx
   import { useParams } from 'react-router'
   import { MOCK_PROFILE } from '../data/mockData'
   import { ProfileCard } from '../components/profile/ProfileCard'

   export function ProfilePage() {
     const { userId } = useParams()
     // Phase 0: userId に関わらず MOCK_PROFILE を表示する
     const profile = MOCK_PROFILE

     return (
       <div className="min-h-screen bg-gradient-to-b from-pink-50 to-white flex flex-col items-center justify-start pt-12 px-4">
         <ProfileCard profile={profile} />
         <p className="mt-6 text-xs text-gray-400">
           {userId ? `ユーザーID: ${userId}` : 'プロフィールページ'}
         </p>
       </div>
     )
   }
   ```

4. `src/pages/LoginMockPage.tsx` を実装で置き換える（useState のみ・DB・OAuth なし）:
   ```tsx
   import { useState } from 'react'
   import { useNavigate } from 'react-router'

   export function LoginMockPage() {
     const [isLoggedIn, setIsLoggedIn] = useState(false)
     const navigate = useNavigate()

     if (isLoggedIn) {
       return (
         <div className="min-h-screen bg-white flex flex-col items-center justify-center gap-6 px-4">
           <div className="text-4xl">✓</div>
           <p className="text-lg font-semibold text-gray-800">ログイン中（デモ）</p>
           <button
             onClick={() => navigate('/')}
             className="px-6 py-3 bg-pink-500 text-white rounded-full font-medium"
           >
             プロフィールを見る
           </button>
           <button
             onClick={() => setIsLoggedIn(false)}
             className="text-sm text-gray-400 underline"
           >
             ログアウト（デモ）
           </button>
         </div>
       )
     }

     return (
       <div className="min-h-screen bg-white flex flex-col items-center justify-center gap-6 px-4">
         <h1 className="text-2xl font-bold text-gray-900">なふだ</h1>
         <p className="text-sm text-gray-500 text-center">
           推し活デジタル名刺アプリ
         </p>
         <button
           onClick={() => setIsLoggedIn(true)}
           className="flex items-center gap-3 px-6 py-3 bg-white border border-gray-300 rounded-full shadow-sm text-gray-700 font-medium hover:bg-gray-50 transition-colors"
         >
           <span>Googleでログイン（デモ）</span>
         </button>
         <p className="text-xs text-gray-400 mt-4 text-center">
           ※ Phase 0 デモ: 実際の認証は行いません
         </p>
       </div>
     )
   }
   ```
  </action>
  <verify>
    <automated>cd /c/Users/pitan/dev/nafuda && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>TypeScript エラーがゼロであること</manual>
  </verify>
  <done>TypeScript エラーゼロ。ProfileCard・SnsLinkList・LoginMockPage が実装済みで ProfilePage が MOCK_PROFILE を参照する</done>
</task>

<task type="auto">
  <name>Task 2: イベントルーム画面（参加者カードアニメーション付き）の実装</name>
  <files>
    src/components/event/ParticipantCard.tsx
    src/components/event/ParticipantList.tsx
    src/pages/EventRoomPage.tsx
  </files>
  <action>
1. `src/components/event/ParticipantCard.tsx` を新規作成する（motion を使い stagger アニメーション）:
   ```tsx
   import { motion } from 'motion/react'
   import { MockProfile } from '../../data/mockData'

   interface ParticipantCardProps {
     profile: MockProfile
     index: number
   }

   export function ParticipantCard({ profile, index }: ParticipantCardProps) {
     return (
       <motion.div
         initial={{ opacity: 0, y: 20 }}
         animate={{ opacity: 1, y: 0 }}
         transition={{ delay: index * 0.12, duration: 0.3, ease: 'easeOut' }}
         className="flex items-center gap-3 p-3 bg-white rounded-xl shadow-sm"
       >
         <img
           src={profile.avatarUrl}
           alt={profile.handle}
           className="w-10 h-10 rounded-full flex-shrink-0"
         />
         <div className="min-w-0">
           <p className="font-medium text-sm text-gray-900 truncate">{profile.handle}</p>
           <div className="flex flex-wrap gap-1 mt-0.5">
             {profile.oshiTags.slice(0, 2).map((tag) => (
               <span
                 key={tag}
                 className="text-xs text-pink-600 bg-pink-50 px-2 py-0.5 rounded-full"
               >
                 {tag}
               </span>
             ))}
           </div>
         </div>
       </motion.div>
     )
   }
   ```

2. `src/components/event/ParticipantList.tsx` を新規作成する:
   ```tsx
   import { MockProfile } from '../../data/mockData'
   import { ParticipantCard } from './ParticipantCard'

   interface ParticipantListProps {
     participants: MockProfile[]
   }

   export function ParticipantList({ participants }: ParticipantListProps) {
     if (participants.length === 0) {
       return (
         <p className="text-center text-gray-400 text-sm py-8">
           まだ参加者がいません
         </p>
       )
     }

     return (
       <div className="flex flex-col gap-2">
         {participants.map((profile, index) => (
           <ParticipantCard key={profile.id} profile={profile} index={index} />
         ))}
       </div>
     )
   }
   ```

3. `src/pages/EventRoomPage.tsx` を完全実装で置き換える。URL パラメータ `:eventId` を使い MOCK_EVENT を表示する。QRボタンは 00-03 プランで追加するため今は省略する:
   ```tsx
   import { useParams } from 'react-router'
   import { MOCK_EVENT } from '../data/mockData'
   import { ParticipantList } from '../components/event/ParticipantList'

   export function EventRoomPage() {
     const { eventId } = useParams()
     // Phase 0: eventId に関わらず MOCK_EVENT を表示する
     const event = MOCK_EVENT

     return (
       <div className="min-h-screen bg-gradient-to-b from-indigo-50 to-white pb-12">
         {/* ヘッダー */}
         <div className="bg-white shadow-sm px-4 py-4 sticky top-0 z-10">
           <h1 className="text-base font-bold text-gray-900 truncate">{event.name}</h1>
           <p className="text-xs text-gray-500 mt-0.5">
             {event.venue} · {event.date}
           </p>
         </div>

         {/* 参加者一覧 */}
         <div className="px-4 pt-4">
           <h2 className="text-sm font-semibold text-gray-600 mb-3">
             参加者 {event.participants.length}人
           </h2>
           <ParticipantList participants={event.participants} />
         </div>

         {/* イベントID表示（デバッグ用） */}
         <p className="text-center text-xs text-gray-300 mt-8">
           {eventId ? `イベントID: ${eventId}` : 'イベントルーム'}
         </p>
       </div>
     )
   }
   ```
  </action>
  <verify>
    <automated>cd /c/Users/pitan/dev/nafuda && npm run build 2>&1 | tail -10</automated>
    <manual>ビルド成功。motion.div が ParticipantCard に存在し stagger アニメーション（delay: index * 0.12）が設定されている。</manual>
  </verify>
  <done>npm run build がエラーなく完了し、ParticipantCard が motion.div アニメーション付きで実装され、EventRoomPage が MOCK_EVENT.participants を ParticipantList に渡して表示する</done>
</task>

</tasks>

<verification>
```bash
cd /c/Users/pitan/dev/nafuda && npx tsc --noEmit && npm run build
```
TypeScript エラーゼロ・ビルド成功を確認する。
</verification>

<success_criteria>
- ProfileCard がハンドル名・アバター・推しタグ・SNSリンクを表示する
- EventRoomPage で参加者カードが motion stagger アニメーション付きで出現する
- LoginMockPage がモックログインボタンを持ち useState のみで状態を管理する（DB・OAuth なし）
- /p/:userId ルートが ProfilePage を表示し MOCK_PROFILE データを使う
- /event/:eventId ルートが EventRoomPage を表示し MOCK_EVENT データを使う
- npm run build がエラーなく完了する
</success_criteria>

<output>
完了後、`.planning/phases/00-prototype/00-02-SUMMARY.md` を作成する。
</output>
