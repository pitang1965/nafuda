---
phase: 00-prototype
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - index.html
  - src/index.css
  - src/main.tsx
  - src/App.tsx
  - src/data/mockData.ts
  - public/_headers
autonomous: true
requirements: []

must_haves:
  truths:
    - "npm run dev が起動し localhost:5173 でアプリが表示される"
    - "npm run build がエラーなく完了し dist/ が生成される"
    - "Tailwind CSS クラスがレンダリングされる（背景色・パディング等が反映される）"
    - "react-router でルーティングが機能し /p/:userId と /event/:eventId が解決される"
    - "モックデータ型 MockProfile・MockEvent が TypeScript エラーなく import できる"
  artifacts:
    - path: "vite.config.ts"
      provides: "Vite ビルド設定（react + tailwindcss + VitePWA プラグイン）"
      contains: "VitePWA"
    - path: "src/index.css"
      provides: "Tailwind v4 ベーススタイル"
      contains: "@import \"tailwindcss\""
    - path: "src/data/mockData.ts"
      provides: "MockProfile・MockEvent 型定義とモックデータ定数"
      exports: ["MockProfile", "SnsLink", "MockEvent", "MOCK_PROFILE", "MOCK_EVENT"]
    - path: "src/App.tsx"
      provides: "React Router ルーティング設定"
      contains: "Routes"
    - path: "public/_headers"
      provides: "Cloudflare Pages ヘッダー設定（PWA MIME 等）"
      contains: "application/manifest+json"
  key_links:
    - from: "vite.config.ts"
      to: "tailwindcss plugin"
      via: "@tailwindcss/vite import"
      pattern: "tailwindcss\\(\\)"
    - from: "src/App.tsx"
      to: "src/pages/"
      via: "react-router Routes/Route"
      pattern: "Route path=\"/p/:userId\""
---

<objective>
React + Vite + Tailwind CSS v4 + vite-plugin-pwa のプロジェクトを初期設定し、モックデータ型定義とルーティング骨格を構築する。

Purpose: 後続プランがページ実装に集中できる土台を確立する。設定ミスをここで排除する。
Output: 起動・ビルドが通るプロジェクト骨格、mockData.ts 型定義、ルーター設定
</objective>

<execution_context>
@C:/Users/pitan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pitan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-prototype/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vite + React + TypeScript プロジェクト生成と依存パッケージインストール</name>
  <files>
    package.json
    vite.config.ts
    tsconfig.json
    tsconfig.app.json
    index.html
    src/index.css
    src/main.tsx
  </files>
  <action>
作業ディレクトリ: C:/Users/pitan/dev/nafuda

1. `npm create vite@latest . -- --template react-ts` で現在のディレクトリにプロジェクト生成する。既存ファイルを上書きするか問われたら全て上書きを選択する。

2. 依存パッケージをインストールする:
   ```
   npm install tailwindcss @tailwindcss/vite
   npm install react-router
   npm install qrcode.react
   npm install react-modal-sheet motion
   npm install -D vite-plugin-pwa workbox-window
   npm install @types/node
   ```
   注意: `framer-motion` は使わない（`motion` パッケージに統合済み）。`react-router-dom` も使わない（v7 から `react-router` に統合済み）。

3. `vite.config.ts` を以下の内容で上書きする:
   ```typescript
   import { defineConfig } from 'vite'
   import react from '@vitejs/plugin-react'
   import tailwindcss from '@tailwindcss/vite'
   import { VitePWA } from 'vite-plugin-pwa'

   export default defineConfig({
     plugins: [
       react(),
       tailwindcss(),
       VitePWA({
         registerType: 'autoUpdate',
         manifest: {
           name: 'なふだ',
           short_name: 'なふだ',
           description: 'QRでその場でつながれる推し活デジタル名刺',
           theme_color: '#ffffff',
           background_color: '#ffffff',
           display: 'standalone',
           start_url: '/',
           icons: [
             { src: '/icons/icon-192.png', sizes: '192x192', type: 'image/png' },
             { src: '/icons/icon-512.png', sizes: '512x512', type: 'image/png' },
           ],
         },
         workbox: {
           clientsClaim: true,
           skipWaiting: true,
         },
         devOptions: { enabled: true },
       }),
     ],
   })
   ```

4. `src/index.css` を以下の内容で置き換える（Tailwind v3 のディレクティブを削除する）:
   ```css
   @import "tailwindcss";
   ```
   注意: `@tailwind base;` `@tailwind components;` `@tailwind utilities;` は v4 では不要。

5. `src/main.tsx` を以下の内容で置き換える:
   ```tsx
   import { StrictMode } from 'react'
   import { createRoot } from 'react-dom/client'
   import { BrowserRouter } from 'react-router'
   import App from './App'
   import './index.css'

   createRoot(document.getElementById('root')!).render(
     <StrictMode>
       <BrowserRouter>
         <App />
       </BrowserRouter>
     </StrictMode>,
   )
   ```
  </action>
  <verify>
    <automated>cd /c/Users/pitan/dev/nafuda && npm run build 2>&1 | tail -5</automated>
    <manual>ビルドが "built in" のメッセージで終了し dist/ が生成されること</manual>
  </verify>
  <done>npm run build がエラーなく完了し dist/ ディレクトリが生成される</done>
</task>

<task type="auto">
  <name>Task 2: App.tsx ルーター設定・mockData.ts 型定義・public/_headers 作成</name>
  <files>
    src/App.tsx
    src/data/mockData.ts
    src/pages/ProfilePage.tsx
    src/pages/EventRoomPage.tsx
    src/pages/LoginMockPage.tsx
    public/_headers
    public/icons/.gitkeep
  </files>
  <action>
1. `src/App.tsx` を以下の内容で置き換える:
   ```tsx
   import { Routes, Route } from 'react-router'
   import { ProfilePage } from './pages/ProfilePage'
   import { EventRoomPage } from './pages/EventRoomPage'
   import { LoginMockPage } from './pages/LoginMockPage'

   export default function App() {
     return (
       <Routes>
         <Route path="/" element={<ProfilePage />} />
         <Route path="/p/:userId" element={<ProfilePage />} />
         <Route path="/event/:eventId" element={<EventRoomPage />} />
         <Route path="/login" element={<LoginMockPage />} />
       </Routes>
     )
   }
   ```

2. `src/data/mockData.ts` を新規作成する:
   ```typescript
   export interface SnsLink {
     platform: 'x' | 'instagram' | 'discord' | 'line_openchat' | 'tiktok' | 'youtube'
     url: string
     handle: string
   }

   export interface MockProfile {
     id: string
     handle: string
     avatarUrl: string
     oshiTags: string[]
     snsLinks: SnsLink[]
     profileUrl: string
   }

   export interface MockEvent {
     id: string
     name: string
     venue: string
     date: string
     participants: MockProfile[]
   }

   export const MOCK_PROFILE: MockProfile = {
     id: 'mock-user-001',
     handle: 'すみれ🌸',
     avatarUrl: 'https://api.dicebear.com/9.x/bottts/svg?seed=mock001',
     oshiTags: ['#田中推し', '#星降る夜のライブ', '#Aグループ'],
     snsLinks: [
       { platform: 'x', url: 'https://x.com/sumire_example', handle: '@sumire_example' },
       { platform: 'instagram', url: 'https://instagram.com/sumire_example', handle: 'sumire_example' },
     ],
     profileUrl: 'https://nafuda.pages.dev/p/mock-user-001',
   }

   export const MOCK_PARTICIPANTS: MockProfile[] = [
     MOCK_PROFILE,
     {
       id: 'mock-user-002',
       handle: 'あおい⭐',
       avatarUrl: 'https://api.dicebear.com/9.x/bottts/svg?seed=mock002',
       oshiTags: ['#田中推し', '#推し活'],
       snsLinks: [
         { platform: 'instagram', url: 'https://instagram.com/aoi_example', handle: 'aoi_example' },
       ],
       profileUrl: 'https://nafuda.pages.dev/p/mock-user-002',
     },
     {
       id: 'mock-user-003',
       handle: 'こはる🎵',
       avatarUrl: 'https://api.dicebear.com/9.x/bottts/svg?seed=mock003',
       oshiTags: ['#鈴木推し', '#現場勢'],
       snsLinks: [
         { platform: 'x', url: 'https://x.com/koharu_example', handle: '@koharu_example' },
       ],
       profileUrl: 'https://nafuda.pages.dev/p/mock-user-003',
     },
   ]

   export const MOCK_EVENT: MockEvent = {
     id: 'event-001',
     name: '星降る夜のライブ2026',
     venue: '東京ドーム',
     date: '2026-04-22',
     participants: MOCK_PARTICIPANTS,
   }
   ```

3. `src/pages/ProfilePage.tsx` をスタブとして新規作成する（後続プランで実装される）:
   ```tsx
   export function ProfilePage() {
     return <div className="p-4 text-gray-800">ProfilePage（実装予定）</div>
   }
   ```

4. `src/pages/EventRoomPage.tsx` をスタブとして新規作成する:
   ```tsx
   export function EventRoomPage() {
     return <div className="p-4 text-gray-800">EventRoomPage（実装予定）</div>
   }
   ```

5. `src/pages/LoginMockPage.tsx` をスタブとして新規作成する:
   ```tsx
   export function LoginMockPage() {
     return <div className="p-4 text-gray-800">LoginMockPage（実装予定）</div>
   }
   ```

6. `public/_headers` を新規作成する:
   ```
   /manifest.webmanifest
     Content-Type: application/manifest+json

   /sw.js
     Cache-Control: no-cache

   /assets/*
     Cache-Control: public, max-age=31536000, immutable

   /workbox-*.js
     Cache-Control: public, max-age=31536000, immutable

   /*
     Cache-Control: no-cache
   ```

7. `public/icons/` ディレクトリを作成し `.gitkeep` を置く（実アイコンは 00-03 プランで追加する）。
  </action>
  <verify>
    <automated>cd /c/Users/pitan/dev/nafuda && npm run build 2>&1 | tail -5 && echo "---" && ls dist/</automated>
    <manual>ビルド成功・dist/ に index.html と assets/ が存在すること。TypeScript エラーがないこと。</manual>
  </verify>
  <done>npm run build がエラーなく完了し、src/data/mockData.ts が MockProfile・MockEvent をエクスポートし、App.tsx に /p/:userId・/event/:eventId・/login ルートが定義されている</done>
</task>

</tasks>

<verification>
```bash
cd /c/Users/pitan/dev/nafuda && npm run build && npx tsc --noEmit
```
TypeScript エラーゼロ・ビルド成功を確認する。
</verification>

<success_criteria>
- npm run build がエラーなく完了し dist/ が生成される
- npx tsc --noEmit がエラーゼロで完了する
- src/index.css に `@import "tailwindcss"` のみが記述されている（v3 ディレクティブなし）
- vite.config.ts に tailwindcss() と VitePWA() プラグインが設定されている
- src/data/mockData.ts が MockProfile・MockEvent 型とモック定数をエクスポートしている
- public/_headers が存在し manifest.webmanifest の Content-Type が設定されている
</success_criteria>

<output>
完了後、`.planning/phases/00-prototype/00-01-SUMMARY.md` を作成する。
</output>
