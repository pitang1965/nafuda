---
phase: 00-prototype
plan: "03"
type: execute
wave: 3
depends_on: ["00-02"]
files_modified:
  - src/components/ui/QRBottomSheet.tsx
  - src/pages/ProfilePage.tsx
  - src/pages/EventRoomPage.tsx
  - public/icons/icon-192.png
  - public/icons/icon-512.png
  - public/favicon.ico
autonomous: false

user_setup:
  - service: cloudflare-pages
    why: "静的ホスティングとデプロイ先"
    dashboard_config:
      - task: "Cloudflare Pages プロジェクトを作成し GitHub リポジトリと接続する"
        location: "Cloudflare Dashboard -> Pages -> Create a project -> Connect to Git"
      - task: "Build settings を設定する: Build command: npm run build / Build output directory: dist"
        location: "Cloudflare Dashboard -> Pages -> プロジェクト -> Settings -> Builds & deployments"

requirements: []

must_haves:
  truths:
    - "ユーザーはモックQRコードを画面上で表示でき、モバイルのネイティブカメラで読み取ってURLに遷移できる"
    - "ユーザーはアプリをホーム画面に追加できる（Cloudflare Pages 上でのPWAマニフェスト）"
    - "プロフィールページの QRボタンをタップするとボトムシートが下から出現する（画面高さ約50%）"
    - "ボトムシート内のQRコードを読み取ると /p/mock-user-001 に遷移する"
    - "Cloudflare Pages へのデプロイが完了し公開URLでアクセスできる"
  artifacts:
    - path: "src/components/ui/QRBottomSheet.tsx"
      provides: "react-modal-sheet + qrcode.react によるQRボトムシート"
      contains: "Sheet"
      exports: ["QRBottomSheet"]
    - path: "public/icons/icon-192.png"
      provides: "PWAインストール用アイコン 192x192"
    - path: "public/icons/icon-512.png"
      provides: "PWAインストール用アイコン 512x512"
  key_links:
    - from: "src/pages/ProfilePage.tsx"
      to: "src/components/ui/QRBottomSheet.tsx"
      via: "useState + onQROpen prop"
      pattern: "QRBottomSheet"
    - from: "src/components/ui/QRBottomSheet.tsx"
      to: "qrcode.react"
      via: "QRCodeSVG コンポーネント"
      pattern: "QRCodeSVG"
    - from: "src/components/ui/QRBottomSheet.tsx"
      to: "react-modal-sheet"
      via: "Sheet コンポーネント snapPoints={[0.5]}"
      pattern: "snapPoints"
---

<objective>
QRコードをボトムシートで表示する UI を実装し、PWA アイコンを配置して Cloudflare Pages にデプロイする。

Purpose: Phase 0 成功基準 3・5（QR表示・PWAインストール）を達成し、ステークホルダーが実機で全 UX フローを確認できる状態にする。
Output: QRBottomSheet コンポーネント、ProfilePage/EventRoomPage への QR 接続、PWA アイコン、Cloudflare Pages 公開 URL
</objective>

<execution_context>
@C:/Users/pitan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pitan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-prototype/00-RESEARCH.md
@.planning/phases/00-prototype/00-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: QRBottomSheet コンポーネント実装と ProfilePage・EventRoomPage への接続</name>
  <files>
    src/components/ui/QRBottomSheet.tsx
    src/pages/ProfilePage.tsx
    src/pages/EventRoomPage.tsx
  </files>
  <action>
1. `src/components/ui/QRBottomSheet.tsx` を新規作成する:
   - react-modal-sheet の Sheet コンポーネントを使う（`react-modal-sheet` の v5.6.0 API）
   - snapPoints={[0.5]} で画面高さ約50%のハーフモーダルにする
   - QRCodeSVG でロゴなし・size=220・marginSize=4・level="M" のQRを表示する
   - Sheet.Backdrop onTap でバックグラウンドタップ時に閉じる
   - 閉じるボタンを Sheet.Content 内下部に配置する
   - ユーザー決定: QR画面からのURL共有・画像保存ボタンは不要（Phase 3 で対応）
   - ユーザー決定: QR内部へのロゴ埋め込みなし（読み取り精度を優先）

   ```tsx
   import Sheet from 'react-modal-sheet'
   import { QRCodeSVG } from 'qrcode.react'

   interface QRBottomSheetProps {
     isOpen: boolean
     onClose: () => void
     url: string
     title?: string
   }

   export function QRBottomSheet({ isOpen, onClose, url, title }: QRBottomSheetProps) {
     return (
       <Sheet
         isOpen={isOpen}
         onClose={onClose}
         snapPoints={[0.5]}
         initialSnap={0}
       >
         <Sheet.Container>
           <Sheet.Header />
           <Sheet.Content>
             <div className="flex flex-col items-center gap-4 px-6 pb-8 pt-2">
               {title && (
                 <p className="text-sm text-gray-500">{title}</p>
               )}
               <div className="bg-white p-4 rounded-2xl shadow-inner">
                 <QRCodeSVG
                   value={url}
                   size={220}
                   marginSize={4}
                   level="M"
                   bgColor="#FFFFFF"
                   fgColor="#000000"
                 />
               </div>
               <p className="text-xs text-gray-400 text-center break-all max-w-xs">
                 {url}
               </p>
               <button
                 onClick={onClose}
                 className="mt-2 text-sm text-gray-400 underline"
               >
                 閉じる
               </button>
             </div>
           </Sheet.Content>
         </Sheet.Container>
         <Sheet.Backdrop onTap={onClose} />
       </Sheet>
     )
   }
   ```

2. `src/pages/ProfilePage.tsx` を更新して QRBottomSheet を接続する。useState で isQROpen を管理し ProfileCard の onQROpen に渡す:
   ```tsx
   import { useState } from 'react'
   import { useParams } from 'react-router'
   import { MOCK_PROFILE } from '../data/mockData'
   import { ProfileCard } from '../components/profile/ProfileCard'
   import { QRBottomSheet } from '../components/ui/QRBottomSheet'

   export function ProfilePage() {
     const { userId } = useParams()
     const profile = MOCK_PROFILE
     const [isQROpen, setIsQROpen] = useState(false)

     return (
       <div className="min-h-screen bg-gradient-to-b from-pink-50 to-white flex flex-col items-center justify-start pt-12 px-4">
         <ProfileCard
           profile={profile}
           onQROpen={() => setIsQROpen(true)}
         />
         <p className="mt-6 text-xs text-gray-400">
           {userId ? `ユーザーID: ${userId}` : 'プロフィールページ'}
         </p>

         <QRBottomSheet
           isOpen={isQROpen}
           onClose={() => setIsQROpen(false)}
           url={profile.profileUrl}
           title="QRコードで共有"
         />
       </div>
     )
   }
   ```

3. `src/pages/EventRoomPage.tsx` を更新して QRボタンと QRBottomSheet を追加する。イベントルームでも自分のモックプロフィール QR を表示できるようにする:
   - ページ上部のヘッダーに「QRコード」ボタンを追加する
   - MOCK_PROFILE.profileUrl を QRBottomSheet に渡す

   ```tsx
   import { useState } from 'react'
   import { useParams } from 'react-router'
   import { MOCK_EVENT, MOCK_PROFILE } from '../data/mockData'
   import { ParticipantList } from '../components/event/ParticipantList'
   import { QRBottomSheet } from '../components/ui/QRBottomSheet'

   export function EventRoomPage() {
     const { eventId } = useParams()
     const event = MOCK_EVENT
     const [isQROpen, setIsQROpen] = useState(false)

     return (
       <div className="min-h-screen bg-gradient-to-b from-indigo-50 to-white pb-12">
         <div className="bg-white shadow-sm px-4 py-4 sticky top-0 z-10 flex items-center justify-between">
           <div className="min-w-0">
             <h1 className="text-base font-bold text-gray-900 truncate">{event.name}</h1>
             <p className="text-xs text-gray-500 mt-0.5">
               {event.venue} · {event.date}
             </p>
           </div>
           <button
             onClick={() => setIsQROpen(true)}
             className="flex-shrink-0 ml-3 px-3 py-1.5 bg-indigo-50 hover:bg-indigo-100 text-indigo-600 rounded-full text-sm font-medium transition-colors"
           >
             QRコード
           </button>
         </div>

         <div className="px-4 pt-4">
           <h2 className="text-sm font-semibold text-gray-600 mb-3">
             参加者 {event.participants.length}人
           </h2>
           <ParticipantList participants={event.participants} />
         </div>

         <p className="text-center text-xs text-gray-300 mt-8">
           {eventId ? `イベントID: ${eventId}` : 'イベントルーム'}
         </p>

         <QRBottomSheet
           isOpen={isQROpen}
           onClose={() => setIsQROpen(false)}
           url={MOCK_PROFILE.profileUrl}
           title="自分のQRコード"
         />
       </div>
     )
   }
   ```
  </action>
  <verify>
    <automated>cd /c/Users/pitan/dev/nafuda && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
    <manual>TypeScript エラーゼロ・ビルド成功。</manual>
  </verify>
  <done>TypeScript エラーゼロ・npm run build 成功。QRBottomSheet が snapPoints={[0.5]} で実装され ProfilePage・EventRoomPage に接続されている</done>
</task>

<task type="auto">
  <name>Task 2: PWA アイコン生成・配置と Cloudflare Pages デプロイ</name>
  <files>
    public/icons/icon-192.png
    public/icons/icon-512.png
    public/favicon.ico
  </files>
  <action>
1. DiceBear API を使って PWA アイコン画像を生成・ダウンロードする。以下の方法を順に試す:

   **方法A - curl で直接ダウンロード（DiceBear が PNG エンドポイントを提供）:**
   ```bash
   curl -o public/icons/icon-192.png "https://api.dicebear.com/9.x/bottts/png?seed=nafuda&size=192"
   curl -o public/icons/icon-512.png "https://api.dicebear.com/9.x/bottts/png?seed=nafuda&size=512"
   ```

   **方法B - Node.js でプレースホルダー PNG を生成（方法Aが失敗した場合の代替）:**
   外部ダウンロードが失敗した場合は以下で 1x1 ピクセルの最小 PNG バイナリを書き込む（デプロイを通すための暫定措置。Lighthouse の PWA スコアは下がるがインストール動作確認は可能）:
   ```bash
   node -e "
   const fs = require('fs');
   const minPng = Buffer.from('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNkYPhfDwAChwGA60e6kgAAAABJRU5ErkJggg==', 'base64');
   fs.writeFileSync('public/icons/icon-192.png', minPng);
   fs.writeFileSync('public/icons/icon-512.png', minPng);
   console.log('Placeholder icons created');
   "
   ```

2. favicon.ico を public/ に配置する:
   ```bash
   curl -o public/favicon.ico "https://api.dicebear.com/9.x/bottts/png?seed=nafuda&size=32" 2>/dev/null || \
   node -e "const fs=require('fs'); fs.writeFileSync('public/favicon.ico', Buffer.from('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNkYPhfDwAChwGA60e6kgAAAABJRU5ErkJggg==', 'base64'));"
   ```

3. 最終ビルドを実行する:
   ```bash
   cd /c/Users/pitan/dev/nafuda && npm run build
   ```

4. Wrangler CLI を使って Cloudflare Pages にデプロイする:
   ```bash
   # Wrangler のインストール確認（未インストールの場合はインストール）
   npx wrangler --version 2>/dev/null || npm install -D wrangler

   # Cloudflare Pages へデプロイ（プロジェクト名: nafuda）
   npx wrangler pages deploy dist --project-name nafuda
   ```
   注意: Wrangler が認証を求めた場合（`wrangler login` が必要）、ブラウザが自動で開く。ログインを完了してから再実行する。

5. デプロイ完了後に表示される公開 URL（例: `https://nafuda.pages.dev` または `https://{hash}.nafuda.pages.dev`）を記録する。
  </action>
  <verify>
    <automated>cd /c/Users/pitan/dev/nafuda && npm run build 2>&1 | tail -5 && ls public/icons/</automated>
    <manual>dist/ にビルド成果物が存在し public/icons/ に icon-192.png と icon-512.png が存在する。</manual>
  </verify>
  <done>npm run build 成功・dist/ 生成済み・public/icons/ にアイコンが存在する・Cloudflare Pages デプロイが完了し公開 URL が得られる</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 実機による全 UX フロー確認（QR・PWA・全画面）</name>
  <action>スマートフォン実機でデプロイ済み URL を開き、QRボトムシート表示・参加者アニメーション・モックログイン・PWA インストールプロンプトの全フローを確認する。</action>
  <verify>
    <automated>cd /c/Users/pitan/dev/nafuda && npm run build 2>&1 | tail -3</automated>
    <manual>スマートフォンのカメラで QR を読み取り /p/mock-user-001 に遷移できること・「ホーム画面に追加」が表示されること</manual>
  </verify>
  <done>Phase 0 の全 5 つの成功基準が実機で確認できた</done>
  <what-built>
    - QRBottomSheet（react-modal-sheet + qrcode.react）をプロフィールページとイベントルームに接続した
    - PWA アイコン（192x192・512x512）を配置し Cloudflare Pages にデプロイした
    - vite-plugin-pwa による PWA マニフェストが Cloudflare Pages で配信される
  </what-built>
  <how-to-verify>
    デプロイ完了後に表示された公開 URL をスマートフォンのブラウザで開いて以下を確認する:

    1. **QR 表示** (成功基準 3):
       - トップページ（プロフィールページ）の「QRコードを表示」ボタンをタップする
       - 画面下半分にボトムシートがスライドイン表示されること
       - ボトムシート内に QR コードが表示されること
       - スマートフォンの標準カメラアプリで QR を読み取り URL `https://nafuda.pages.dev/p/mock-user-001` に遷移できること

    2. **プロフィールカード** (成功基準 1):
       - ハンドル名「すみれ🌸」・アバター画像・推しタグ・SNS リンクボタンが表示されること

    3. **イベントルーム** (成功基準 2):
       - `/event/event-001` にアクセスして参加者カードがアニメーション付きで出現すること
       - ヘッダーの「QRコード」ボタンをタップするとボトムシートが開くこと

    4. **モックログイン** (成功基準 4):
       - `/login` にアクセスして「Googleでログイン（デモ）」ボタンをタップするとログイン状態に切り替わること

    5. **PWA インストール** (成功基準 5):
       - ブラウザのアドレスバーや共有メニューに「ホーム画面に追加」が表示されること（iOS Safari: 共有 → ホーム画面に追加 / Android Chrome: メニュー → アプリをインストール）
  </how-to-verify>
  <resume-signal>全項目確認後「approved」と入力するか、問題があれば内容を記述してください</resume-signal>
</task>

</tasks>

<verification>
```bash
cd /c/Users/pitan/dev/nafuda && npx tsc --noEmit && npm run build
```
TypeScript エラーゼロ・ビルド成功を確認する。デプロイ後は Lighthouse PWA 診断（DevTools -> Lighthouse -> Progressive Web App）を実行して「Installable」が表示されることを確認する。
</verification>

<success_criteria>
- QRBottomSheet が snapPoints={[0.5]}（画面高さ 50%）のボトムシートで QRCodeSVG を表示する
- ボトムシートはバックグラウンドタップまたは「閉じる」ボタンで閉じられる
- QR 内に埋め込まれた URL をカメラで読み取ると /p/mock-user-001 に遷移できる（Cloudflare Pages 上で動作）
- PWA マニフェストが application/manifest+json で配信される（_headers による MIME 設定）
- ブラウザの「ホーム画面に追加」オプションが表示される
- Cloudflare Pages のデプロイが完了し公開 URL でアクセスできる
</success_criteria>

<output>
完了後、`.planning/phases/00-prototype/00-03-SUMMARY.md` を作成する。
</output>
